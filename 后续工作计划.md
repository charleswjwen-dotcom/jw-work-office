# My-work-office 后续工作计划与过程质量把控

> 版本：v1.1
> 日期：2026-09-10
> 状态：待评审确认
> 关联文档：《需求文档.md》（PRD v2.1，第 8 节待确认事项已全部关闭）
> 本文回答一个问题——**"正式需求已定，接下来怎么一步步、有质量地把它做出来？"**

---

## 0. 阅读指引

- 本文档只讲"后续怎么做"与"如何保证做得对、做得好"，不重复需求内容。
- 主线有两条：**① 阶段推进路线（第 2 节）** 与 **② 贯穿全程的过程质量体系（第 3 节）**。
- 每个阶段都设有**质量门禁（Quality Gate）**：不达标不进入下一阶段，这是过程质量把控的核心机制。

---

## 1. 总体原则

| 原则 | 含义 |
| --- | --- |
| 需求驱动 | 一切开发对齐 PRD v2.0，尤其 3.2.5 验收标准、4A 技术假设、7A NFR |
| 门禁前置 | 每阶段有明确的准入 / 准出标准，未通过评审不流入下一阶段 |
| 风险先行 | 高不确定性事项（Word「页」定位、开源库保真度、AI 可控性）先做技术验证 |
| 小步验证 | 优先打通"最小可信闭环"（导入→对话→diff 预览→确认→回溯），再横向扩能力 |
| 质量内建 | 质量不是测试阶段才管，而是编码、评审、AI 输出各环节内建 |
| 可回退 | 每个里程碑产物可评审、可回退，避免带病推进 |

---

## 2. 阶段推进路线（阶段 + 里程碑 + 质量门禁）

> 分期对齐 PRD 第 6.3 节：一期 MVP → 二期能力增强 → 三期专业化与扩展。
> 下表每个阶段均给出【准入】【核心工作】【交付物】【准出门禁】。

### 阶段 S0：需求确认与技术验证（PoC）

- **准入**：PRD v2.1 已成文，第 8 节待确认事项已全部关闭。
- **核心工作**：
  1. ~~关闭 PRD 第 8 节待确认事项~~（✅ 已于 PRD v2.1 完成：macOS 优先 / 本地单机+预留云端与团队共享 / LibreOffice 提升保真 / 上千文件规模并预留海量 / PPT 支持大纲整套生成+优化 / AI 生成配图 / 投标覆盖对点·废标·目录·合规）。
  2. 高风险技术点 PoC：Word「页」定位映射（4A.2）、开源库保真度、LibreOffice 兜底、LLM 工具调用可控性、AI 生成配图可用性。
  3. 确定性 vs 生成性操作边界的可行性验证（4A.1）。
- **交付物**：技术可行性报告、风险清单（含缓解方案）。
- **准出门禁 ✅**：
  - 待确认事项全部有结论（✅ 已达成）；
  - 每个高风险点有"可行 / 有兜底 / 或降级方案"明确结论；
  - PoC 结论经需求方确认。

### 阶段 S1：架构与详细设计

- **准入**：S0 门禁通过。
- **核心工作**：
  1. 技术架构设计（对齐 PRD 第 4 章）：模块划分、Electron 主/渲染进程边界、数据流。
  2. **工具契约与 Tool Registry 设计**（4B）：接口规范、插件扩展点、MCP 兼容预留。
  3. 数据模型落地设计（PRD 第 5 章）：工作空间、会话持久化、ChangeSet、版本存储策略、SQLite 表结构。
  4. LLM 适配层设计（OpenAI 兼容 + Anthropic，本地模型 Ollama/vLLM 接入）。
  5. UI/UX 详细设计（对标 Claude Cowork，三栏 + 信任交互 2A 的 diff 预览形态）。
- **交付物**：架构设计文档、接口/数据模型定义、UI 高保真设计稿、技术选型清单。
- **准出门禁 ✅**：
  - 架构评审通过（含性能/隐私/可靠性 NFR 可达性论证，7A）；
  - 工具契约与数据模型经评审冻结为基线；
  - 设计稿覆盖全部 P0 交互路径。

### 阶段 S2：原型（可交互 Demo）

- **准入**：S1 门禁通过。
- **核心工作**：搭建骨架工程 + 打通**最小可信闭环**：导入一个文件 → 对话下达一条确定性指令 → 生成 ChangeSet → diff 预览 → 确认应用 → 快照 → 回溯。
- **交付物**：可运行原型、闭环演示、原型评审反馈记录。
- **准出门禁 ✅**：
  - 最小可信闭环端到端跑通；
  - 关键交互（预览/确认/部分接受/回退）体验被需求方认可；
  - 无阻断性技术障碍。

### 阶段 S3：一期 MVP 开发

- **准入**：S2 门禁通过。
- **核心工作**（对齐 PRD 6.3 一期）：
  1. 文件导入管理 + 元信息 + 本地检索（3.1）；
  2. 三栏界面（2）；
  3. Word/Excel/PPT 的 **P0 能力**，以确定性代码实现（3.2 + 4A.1）；
  4. 信任交互流程（2A，基于 ChangeSet）；
  5. 快照 + 线性回溯（3.3）；
  6. 多模型配置（3.5）；
  7. 核心 NFR 落地：性能基线、隐私红线（数据流向透明/密钥安全）、操作原子性（7A）。
- **交付物**：可安装的 MVP 版本、单元/集成测试、用户手册草稿。
- **准出门禁 ✅**：
  - **PRD 3.2.5 全部 P0 验收标准通过**；
  - NFR 关键指标达标（冷启动/导入/检索/确定性操作性能，7A.1；隐私红线 7A.2；原子性 7A.3）；
  - 核心链路测试覆盖达标、无 P0/P1 缺陷未闭环。

### 阶段 S4：一期测试与发布

- **准入**：S3 门禁通过。
- **核心工作**：系统测试、NFR 专项测试（性能/隐私/可靠性）、真实用户 UAT、缺陷收敛、发布包构建与签名。
- **交付物**：测试报告、UAT 结论、发布包、发布说明、回滚预案。
- **准出门禁 ✅**：
  - 全部 P0/P1 缺陷闭环；
  - UAT 通过（真实办公文件跑通、用户敢用）；
  - 隐私合规红线复核通过；
  - 发布与回滚预案就绪。

### 阶段 S5：二期能力增强

- **核心工作**（对齐 PRD 6.3 二期）：模板/风格匹配（3.2.4）、PPT 美化、Excel 可视化增强、显式偏好记忆与有限自检（3.4 A/B）、半确定性/生成性操作的多方案与迭代、范围定位机制完善（4A.2 含 Word「页」映射与 LibreOffice 兜底）、可观测性（7A.4）。
- **准出门禁 ✅**：新增能力有验收标准并通过；偏好记忆的召回/失效可控；生成性操作满足"可用 + 多方案 + 可拒绝回退"。

### 阶段 S6：三期专业化与扩展

- **核心工作**（对齐 PRD 6.3 三期）：投标文件专业模块（以插件接入 Tool Registry，4B.2）、批量处理、自动进化（附度量验证，3.4 C）、云端协作扩展与 MCP 兼容。
- **准出门禁 ✅**：投标模块作为插件独立验收；自动进化有度量证据方可开启；扩展不破坏既有契约与隐私红线。

### 里程碑一览

| 里程碑 | 标志 | 对应阶段 |
| --- | --- | --- |
| M0 需求冻结 | PRD 签署、待确认事项关闭（✅ PRD v2.1 已完成） | S0 |
| M1 设计冻结 | 架构/契约/数据模型基线确立 | S1 |
| M2 闭环验证 | 最小可信闭环跑通 | S2 |
| M3 MVP 就绪 | P0 验收标准全通过 | S3 |
| M4 一期发布 | UAT 通过、正式发布 | S4 |
| M5 能力增强 | 二期功能上线 | S5 |
| M6 专业化 | 投标模块等插件上线 | S6 |

---

## 3. 贯穿全程的过程质量体系

> 这是"过程质量把控"的落点：不依赖最后测试兜底，而在每个环节内建质量。

### 3.1 需求与设计质量

- 需求可追溯：每个开发任务反向关联到 PRD 章节（用第 9 节覆盖表核对）。
- 变更走评审：需求/设计变更需评估影响面并更新文档版本，禁止口头改需求。
- 设计冻结：契约与数据模型经评审后作为基线，变更需版本化。

### 3.2 代码质量

- 编码规范 + Lint/格式化强制，提交前静态检查。
- **代码评审（Code Review）**：所有合并需至少一名评审通过。
- 确定性操作与 LLM 调用严格分层，边界清晰（对齐 4A.1）。
- 密钥与文档内容处理遵守隐私红线，评审重点核查（7A.2）。

### 3.3 测试质量

| 层级 | 内容 | 关卡 |
| --- | --- | --- |
| 单元测试 | 确定性操作、数据模型、工具契约 | 核心模块覆盖达标 |
| 集成测试 | 导入→处理→ChangeSet→预览→应用→回溯闭环 | 关键链路必测 |
| NFR 专项 | 性能、隐私流向、可靠性/原子性 | 对齐 7A 指标 |
| UAT | 真实办公文件、真实用户操作 | 发布前必过 |
| 回归测试 | 每次迭代对既有验收标准回归 | 防止能力退化 |

### 3.4 AI 输出质量（本产品特有）

- **可控性**：生成性操作必须经 diff 预览 + 用户确认，禁止静默改文件（2A）。
- **可回退**：任何 AI 操作都有 ChangeSet 与快照，支持部分接受与整体回退（3.3）。
- **效果度量**：按 3.4 C 建立采纳率/回退率/重试率等指标，作为自进化开启的前置证据。
- **降级策略**：模型故障或输出不可信时有兜底（7A.3），不阻断用户主流程。

### 3.5 风险与变更管理

- 维护动态风险清单（对齐 PRD 第 7 章），每里程碑复盘一次。
- 高风险项（保真度、Word 分页、AI 可控性）设专门验证与兜底方案。
- 变更管理：范围/进度/技术选型变更均登记、评估、评审后执行。

### 3.6 文档与交付质量

- 每阶段交付物齐备方可过门禁（见第 2 节各阶段交付物）。
- 关键文档（架构、契约、数据模型、测试报告、发布说明）版本化留存。
- 用户手册与发布说明随版本同步更新。

---

## 4. 质量门禁总览（速查）

| 阶段 | 准出核心门禁（一句话） |
| --- | --- |
| S0 | 待确认事项关闭 + 高风险点有结论 + 需求签署 |
| S1 | 架构/契约/数据模型评审冻结 + NFR 可达性论证 |
| S2 | 最小可信闭环跑通 + 交互体验获认可 |
| S3 | PRD 3.2.5 全部 P0 验收标准通过 + NFR 达标 |
| S4 | 缺陷闭环 + UAT 通过 + 隐私复核 + 回滚预案就绪 |
| S5 | 新增能力有验收并通过 + 生成性操作可控可回退 |
| S6 | 插件独立验收 + 自动进化有度量证据 + 不破坏契约与红线 |

---

## 5. 建议的下一步动作

1. ~~关闭 PRD 第 8 节待确认事项~~（✅ 已完成，PRD 升级至 v2.1）。
2. **评审本计划**，确认阶段划分与门禁标准是否符合预期。
3. **进入 S0**：先做高风险技术点 PoC（Word「页」定位、开源库保真度、LibreOffice 兜底、LLM 可控性、AI 配图），出技术可行性报告与风险清单；同时可并行展开 S1 架构设计文档。

---

*文档结束。本计划与《需求文档.md》配套使用，作为项目推进与过程质量把控的依据。*
