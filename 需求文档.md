# My-work-office 需求文档（PRD）

> 版本：v2.1
> 日期：2026-09-10
> 文档状态：需求已确认
> 产品定位：一款带界面的、可处理 / 生成 / 优化 Word、PPT、Excel 文件的交互式桌面 Agent

> **v2.0 更新说明**：在 v1.0 基础上，针对"AI Agent 类产品"的特殊性与真实用户工作流，补齐五块关键内容——
> ① 核心技术假设（第 4A 节：确定性代码 vs LLM 的操作分类、文件范围定位机制）；
> ② 信任交互流程（第 2A 节：diff 预览 / 确认 / 部分接受 / 澄清追问）；
> ③ 非功能性需求 NFR（第 7A 节：性能 / 隐私合规 / 可靠性 / 可观测性）；
> ④ P0 功能验收标准（各功能表补充「验收标准」）与升级后的数据模型（引入工作空间 / 会话持久化 / 版本存储策略）；
> ⑤ 工具契约规范与插件化扩展（第 4B 节）。

> **v2.1 更新说明**：需求方已就 v2.0 第 8 节全部 7 项待确认事项给出明确结论，本版据此关闭第 8 节（转为「已确认事项」），并同步更新第 0 节决策表及正文相关标注。核心结论：目标平台 macOS 优先（后续扩展 Windows / 不排除其它 OS 迁移）；本地单机优先并预留云端与团队共享扩展；预览引入 LibreOffice 提升保真；数据规模按上千文件设计并预留海量能力；PPT 支持从零按大纲生成整套 + 优化已有；美化需 AI 生成配图能力；投标模块覆盖招标对点 / 废标项检查 / 应标目录 / 格式合规校验，面向工程 / 政府采购 / IT 服务类。

---

## 0. 文档说明

本文档为产品需求文档（PRD），仅描述"做什么"与"为什么"，不包含具体代码实现。技术选型部分给出方向性约束，供后续架构设计与原型阶段落地。

**已与需求方确认的关键决策：**

| 决策项 | 结论 |
| --- | --- |
| 技术形态 | 桌面应用（Electron 技术栈） |
| 文件处理引擎 | 以开源库为主 |
| 当前阶段交付物 | 先确定需求文档，后续再开展原型设计与开发 |
| 大模型接入 | 优先兼容 OpenAI 兼容格式 + Claude/Anthropic |
| 投标文件处理 | 先做通用能力，投标作为后续扩展模块预留接口；覆盖招标对点 / 废标项检查 / 应标目录 / 格式合规校验，面向工程 / 政府采购 / IT 服务类 |
| 版本回溯 | 快照 + 线性回溯 |
| 进化/反思 | 用户反馈学习 + 任务级结果自检自优化，两者都要 |
| 部署形态 | 本地单机优先，预留云端与团队共享扩展 |
| 操作系统 | macOS 优先，后续扩展 Windows，不排除后续基于源码迁移至其它 OS |
| 数据规模 | 按上千个文件规模设计，并预留后续海量文件支持能力 |
| PPT 生成 | 支持从零按大纲生成整套 PPT，同时支持优化调整已有 PPT |
| 图像生成 | 支持 AI 生成配图能力，用于 PPT / 文档美化 |
| 预览保真度 | 尽量真实还原，引入 LibreOffice 依赖提升保真 |
| 界面风格 | 参考 Claude Cowork，功能不完善处对标其补齐 |

---

## 1. 产品概述

### 1.1 一句话定位

My-work-office 是一款运行在桌面端的智能办公 Agent。用户导入 Word / PPT / Excel 文件后，通过自然语言对话即可让 AI 完成文件的处理、生成、排版优化与数据可视化，全过程结果可预览、可追溯、可恢复。

### 1.2 目标用户

- 需要高频处理办公文档的知识工作者（文案、行政、运营、分析师）
- 需要规范化排版与格式统一的团队（如标书、报告、汇报材料撰写者）
- 希望借助 AI 降低重复性文档操作成本的个人用户

### 1.3 核心价值

1. **对话即操作**：把繁琐的 Office 手工操作转化为一句自然语言指令。
2. **所见即所得**：处理过程与结果实时在右侧预览区展示。
3. **可回退无风险**：每次 AI 处理自动快照，随时对比、回溯、恢复。
4. **越用越懂你**：Agent 记忆用户偏好，并对自身结果自检优化。

### 1.4 设计参考

界面与交互对标 **Claude Cowork**：三栏布局、简洁克制的视觉、以对话驱动任务、结果卡片化展示、支持配色主题切换。功能上以 Cowork 的"任务编排 + 工件（Artifact）预览 + 迭代对话"模式为蓝本进行补齐。

---

## 2. 界面与交互设计

### 2.1 整体布局（三栏式）

```
┌──────────────┬─────────────────────────┬──────────────────────┐
│              │                         │                      │
│  左栏         │      中栏                 │      右栏              │
│  文件列表      │   交互式聊天界面           │   文件展示 / 实时处理     │
│              │                         │                      │
│ - 搜索/筛选    │  - 指令输入              │  - 文件在线预览          │
│ - 文件卡片     │  - 处理结果卡片           │  - 处理高亮/diff        │
│ - 分类/标签    │  - 多轮对话历史           │  - 版本切换             │
│ - 导入入口     │  - 任务进度              │  - 页/表/工作表导航      │
│              │                         │                      │
└──────────────┴─────────────────────────┴──────────────────────┘
   顶部栏：模型选择 / 主题配色 / 设置 / 全局搜索
```

### 2.2 左栏——文件列表

- **导入方式**：拖拽导入、点击选择、批量导入；支持 `.docx/.doc`、`.pptx/.ppt`、`.xlsx/.xls/.csv`。
- **文件元信息**：导入时自动关联并存储：文件名、文件类型、导入时间戳、最后修改时间、文件大小、页数/工作表数、来源路径、标签、缩略图。
- **快速查找/搜索**：
  - 按文件名、类型、时间范围筛选；
  - 按标签、按内容全文检索（对文档正文建立索引）；
  - 排序（时间、名称、类型、大小）。
- **文件卡片**：展示缩略图、名称、类型图标、时间、版本数角标。

### 2.3 中栏——交互式聊天界面

- 接收自然语言命令，展示 AI 的理解、执行计划、处理结果摘要。
- 结果以**卡片**呈现：变更摘要、"应用/撤销/查看差异/生成新版本"操作按钮。
- 支持多轮上下文对话（引用当前选中文件、指定页/段/单元格作为操作对象）。
- 展示任务进度（解析中、处理中、渲染中、完成）与错误提示。
- 支持指令快捷模板（如"统一字体""生成目录""美化本页 PPT"）。

### 2.4 右栏——文件展示 / 实时处理

- **在线预览**：Word 分页显示、PPT 分页缩略图 + 大图、Excel 表格网格 + 图表。
- **实时处理高亮**：AI 修改的段落/单元格/元素以高亮或 diff 形式标注。
- **导航**：Word 按页/标题跳转、PPT 按页跳转、Excel 按工作表/单元格定位。
- **版本切换**：在预览区顶部切换查看不同版本快照，支持并排对比。

### 2.5 顶部栏 / 设置

- 模型选择器（切换已配置的大模型）。
- 主题与配色切换（浅色/深色/自定义主题色，对标 Cowork 可调配色）。
- 全局设置：API Key 管理、存储路径、索引管理、偏好设置。

---

## 2A. 信任交互流程（核心交互，P0）

> 本节解决真实用户最大的心理障碍——"AI 会不会把我辛苦做的文件改乱"。事后能回退（第 3.3 节快照）不够，必须建立**事前信任**。以下流程为 P0 强制默认行为。

### 2A.1 "预览 → 确认 → 落盘"强制流程

- AI 对文件的任何**写操作**默认不直接落盘，先生成**待确认变更（Pending Change）**。
- 右栏以 **diff 形式高亮**展示变更前后对比（Word 段落级、PPT 元素级、Excel 单元格级）。
- 用户在中栏结果卡片上选择「应用」「放弃」「部分应用」；仅在「应用」后才写入并生成新版本快照。
- 提供全局开关"信任模式"：熟练用户可开启"自动应用"，但破坏性操作（删除、全局替换、批量）**始终强制确认**，不受开关影响。

### 2A.2 部分接受（Partial Accept）

- 当一条指令产生多处变更时（如"全文统一字体"影响 200 段），变更以**可勾选清单**呈现。
- 用户可逐项 / 分组勾选接受，未勾选项保持原样。
- 部分接受后生成的版本快照仅包含被接受的变更。

### 2A.3 变更解释（Why）

- 每处变更附带 AI 的**简短意图说明**（如"此处将'的的'改为'的'——修正重复助词"）。
- 让用户看得懂"为什么这么改"，降低采纳门槛。

### 2A.4 指令澄清与歧义追问

- 当指令模糊（如"把这个弄好看点""优化一下"）或范围不明时，AI **先追问澄清**而非贸然执行。
- 追问以选项形式给出（如"你指的是①调整配色 ②重排版式 ③精简文字？"），降低用户表达成本。
- 提供"按你的理解直接做"的快捷选项，兼顾效率。

### 2A.5 执行中可中断 / 可纠偏

- 长任务执行中可**随时中断**，已完成的中间变更仍以待确认形式保留。
- 执行方向错误时，用户可基于中间结果追加指令纠偏，无需从零重来。

---

## 3. 功能需求详述

### 3.1 文件导入与管理（对应需求 1、2）

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| F1-1 | 多格式导入 | 支持 Word/PPT/Excel/CSV，拖拽与批量导入 | P0 |
| F1-2 | 元信息关联 | 自动提取并存储文件名、类型、时间戳、大小、页数等 | P0 |
| F1-3 | 全文索引 | 对文档正文建立本地检索索引 | P0 |
| F1-4 | 搜索与筛选 | 按名称/类型/时间/标签/内容检索 | P0 |
| F1-5 | 标签与分类 | 用户自定义标签，支持分组管理 | P1 |
| F1-6 | 在线预览 | 三栏右侧在线打开与渲染显示 | P0 |
| F1-7 | 文件删除/重命名/导出 | 基础文件管理与结果导出 | P0 |

### 3.2 对话式文件处理（对应需求 3）

以下能力均通过自然语言指令触发，AI 解析后调用对应文件操作引擎执行。

#### 3.2.1 Word 处理

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FW-1 | 统一字体/颜色/字号 | 全文或指定范围统一文字样式 | P0 |
| FW-2 | 段落级编辑 | 处理指定页/指定段落内容（改写、润色、删改） | P0 |
| FW-3 | 统一排版 | 大小标题层级、段落间距、缩进、行距统一 | P0 |
| FW-4 | 目录处理 | 自动生成/更新目录，标题样式规范化 | P0 |
| FW-5 | 页眉页脚/页码 | 统一设置页眉页脚与页码 | P1 |
| FW-6 | 模板/风格匹配 | 参考指定模板或某文件风格进行匹配调整 | P1 |
| FW-7 | 投标文件格式处理 | 招标要求对点、应标目录生成、内容梳理（**后续扩展模块，本期预留接口**） | P2 |

#### 3.2.2 PPT 处理

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FP-1 | 排版调整 | 指定页元素对齐、布局、层次调整 | P0 |
| FP-2 | 内容优化 | 文案精炼、要点提炼、表达优化 | P0 |
| FP-3 | 展示方式调整 | 图文布局、列表/图表化呈现方式转换 | P1 |
| FP-4 | 配色/图像美化 | 统一配色方案、图像风格调整；支持 AI 生成配图（文生图） | P1 |
| FP-5 | 统一样式 | 全篇字体、配色、母版风格统一 | P0 |
| FP-6 | 模板/风格匹配 | 参考模板或某文件风格匹配 | P1 |
| FP-7 | 大纲生成整套 PPT | 从零根据大纲/主题生成完整演示文稿（生成性操作，见 4A.1） | P1 |

#### 3.2.3 Excel 处理

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FE-1 | 单元格/行列编辑 | 处理指定行/列/单元格内容 | P0 |
| FE-2 | 数据处理 | 清洗、去重、格式化、排序、筛选 | P0 |
| FE-3 | 公式与计算 | 生成公式、批量计算、统计汇总 | P0 |
| FE-4 | 结果可视化 | 根据数据生成图表（柱/折/饼等） | P0 |
| FE-5 | 样式统一 | 统一字体、颜色、边框、条件格式 | P1 |
| FE-6 | 模板/风格匹配 | 参考模板或某文件风格匹配 | P1 |

#### 3.2.4 跨格式通用能力

- **模板 / 风格匹配（对应需求 3.8）**：用户可指定一个模板文件或参考文件，Agent 提取其样式规则（字体、配色、间距、母版等）并应用到目标文件。
- **范围定位**：指令可精确指向"第 X 页""某段""某列/行/单元格""某页 PPT"。

#### 3.2.5 P0 功能验收标准（Definition of Done）

> 为避免"做了但无法判定是否达标"，对关键 P0 功能给出可验证的验收标准。确定性操作用自动化断言，生成性操作用人工判据。

| 功能 | 验收标准（满足即视为达成） |
| --- | --- |
| F1-1 多格式导入 | 主流 `.docx/.pptx/.xlsx/.csv` 导入成功率 ≥ 99%；导入失败有明确原因提示；不崩溃 |
| F1-2 元信息关联 | 文件名/类型/时间/大小/页数(表数) 100% 正确提取并可检索 |
| F1-4 搜索与筛选 | 按名称/类型/时间/内容检索，命中结果准确；万级以内文件检索响应 < 1s |
| F1-6 在线预览 | Word/PPT/Excel 可打开渲染；基础排版（字体、表格、图片位置）无明显错位 |
| FW-1 统一字体/字号 | 指定范围内所有文字样式**确定性统一**，可用规则断言校验；未指定范围不被误改 |
| FW-4 目录处理 | 生成的目录标题层级正确、页码/链接可跳转、更新后连续无缺漏 |
| FE-3 公式与计算 | 生成公式计算结果与预期一致；无 `#REF!`/`#VALUE!` 报错 |
| FE-4 结果可视化 | 依据选定数据正确生成图表，图表类型/坐标轴/数据系列与数据匹配 |
| 通用-信任流程 | 任何写操作先出 diff 预览，用户确认后才落盘；破坏性操作强制确认（见 2A） |
| 通用-版本回溯 | 每次应用变更生成快照；可对比、可一键恢复到任意历史版本，恢复后内容与该版本一致 |

- 生成性操作（润色、PPT 美化等）不设"唯一正确答案"，验收改用：**产出可用（不破坏文件结构）+ 支持多方案/迭代 + 用户可拒绝回退**。

### 3.3 版本管理与追溯（对应需求 4）

- **快照机制**：每次 AI 处理成功后自动生成版本快照，记录处理时间、触发指令、变更摘要。
- **线性回溯**：以时间线呈现版本历史，支持查看任意版本、对比差异、一键恢复到指定版本。
- **差异展示**：版本间以 diff 形式高亮变更内容。
- **手动快照**：用户可在关键节点手动打点保存版本。

### 3.4 Agent 进化、反思与迭代优化（对应需求 6）

> 说明：此类"自进化"需求最易沦为 demo 好看、实际不可控。故本文档明确其**触发条件、判据、存储、召回、失效与度量**，并做分期降级：本期先做"可控的显式偏好记忆 + 有限自检"，"自动进化"放到后期并附带度量验证。

**A. 用户反馈学习 + 偏好记忆（本期 P1，可控优先）**
- **触发**：用户对结果执行"部分接受 / 拒绝 / 手动改回"时，记录该行为与上下文。
- **判据**：同类偏好累计出现 ≥ N 次（阈值可配）才提炼为偏好规则，避免误学。
- **存储**：偏好以**用户可见、可编辑、可删除的规则列表**存在（透明化），而非黑盒。
- **召回**：同类任务执行前自动加载匹配偏好并在卡片中提示"已按你的偏好：标题用微软雅黑"。
- **失效/纠正**：用户可随时关闭或删除某条偏好；连续被推翻的偏好自动降权。

**B. 任务级结果自检 + 自优化（内循环反思，本期做有限版）**
- **自检判据**：优先用**确定性规则校验**（如目录页码是否连续、字体是否真的统一、公式是否报错），而非仅靠 LLM 自评。
- **多方案**：对主观类任务（PPT 美化、文案优化）可生成 2–3 个方案供用户选择，不自动择一。
- **失败重试**：工具执行报错时自动反思错误信息并重试（上限可配），仍失败则如实告知并给出人工建议。

**C. 效果度量（用于验证"是否真在进化"）**
- 记录关键指标：指令一次成功率、变更接受率、平均纠偏轮次、偏好命中率。
- 度量数据本地留存，供后续评估与调优；无度量则不宣称"已进化"。

### 3.5 多模型接入与配置（对应需求 7）

- **API Key 管理**：支持配置多组大模型的 API Key、Base URL、模型名。
- **兼容协议**：
  - OpenAI 兼容格式（覆盖 DeepSeek、通义、Kimi、GLM、本地 vLLM 等大多数模型）；
  - Claude / Anthropic 原生格式；
  - 图像生成模型接入（文生图，供 PPT/文档配图，见 FP-4）。
- **模型切换**：会话中可随时切换当前使用模型。
- **安全存储**：API Key 本地加密存储，不明文记录、不上传。
- **连通性测试**：配置后可测试 Key 与模型可用性。

---

## 4. 技术架构（方向性约束）

> 仅作方向约束，详细设计在后续架构阶段展开。

### 4.1 总体形态

- **桌面应用**：基于 Electron，前端渲染 + 本地 Node 主进程。
- **本地优先**：文件、版本、索引、配置均存储在本地；预留云端同步扩展点。

### 4.2 分层结构

```
┌─────────────────────────────────────────────┐
│  渲染层（UI）：三栏界面 / 主题 / 预览          │
├─────────────────────────────────────────────┤
│  应用层：会话编排 / 指令解析 / 任务调度         │
├─────────────────────────────────────────────┤
│  Agent 层：意图识别 / 工具调用 / 反思循环       │
├─────────────────────────────────────────────┤
│  能力层（工具集）：                            │
│   - Word 引擎   - PPT 引擎   - Excel 引擎      │
│   - 预览渲染    - 版本快照   - 全文索引         │
├─────────────────────────────────────────────┤
│  基础层：本地存储 / 文件系统 / 模型适配器        │
└─────────────────────────────────────────────┘
```

### 4.3 关键技术选型（候选，以开源库为主）

| 领域 | 候选方案 |
| --- | --- |
| 桌面框架 | Electron |
| 前端 | React + TypeScript |
| Word 处理 | docx / mammoth（预览）/ 处理库组合 |
| Excel 处理 | SheetJS(xlsx) / ExcelJS |
| PPT 处理 | pptxgenjs（生成）/ 解析库组合 |
| 在线预览 | 前端渲染 + 可选 LibreOffice 无头转换兜底 |
| 全文索引 | 本地轻量索引（如 lunr / 本地搜索库） |
| 版本存储 | 本地快照文件 + 元数据库（SQLite） |
| 模型接入 | 统一适配器封装 OpenAI 兼容 + Anthropic |

> 注：开源库对复杂排版的保真度有边界（见第 7 节风险）。

### 4.4 Agent 工具调用（Tool/Function Calling）

- Agent 将自然语言指令解析为对文件引擎的结构化操作调用。
- 每个文件操作封装为可被模型调用的工具（如 `setFont`、`generateTOC`、`createChart`）。
- 支持多步任务编排与中间结果反馈。

---

## 4A. 核心技术假设（决定产品可行性的前提）

> 本节明确"哪些操作靠确定性代码、哪些靠 LLM"，以及"如何精确定位文件中的操作范围"。这两点是 AI 办公 Agent 能否可控落地的命门，若不界定清楚，后续实现会陷入"结果不可预期、无法验收"的困境。

### 4A.1 确定性代码 vs LLM 的操作分类

产品将所有文件操作划分为三类，明确各自的执行主体与验收方式：

| 类别 | 说明 | 执行主体 | 举例 | 结果特征 |
| --- | --- | --- | --- | --- |
| **确定性操作** | 规则明确、结果唯一 | 纯代码执行（工具函数），LLM 仅负责解析参数 | 统一字体/字号、生成目录、公式计算、单元格排序、条件格式 | 可 100% 复现，可用规则校验 |
| **半确定性操作** | 规则+主观判断混合 | 代码执行 + LLM 决策部分参数 | 版式规范化（缩进/间距标准由代码定，但"哪些是标题"需 LLM 判断） | 结构可校验，判断可能有偏差 |
| **生成/主观性操作** | 无唯一正解 | LLM 生成内容 + 代码落地 | 文案润色、PPT 美化、要点提炼、配色建议 | 不可复现，需人工确认/多方案选择 |

- **原则**：能用确定性代码做的，绝不交给 LLM"猜"（如字体统一必须是代码遍历设置，而非让模型"重写文档"）。
- **验收差异**：确定性操作可写自动化断言；生成性操作只能靠"用户接受率 + 多方案"来度量，不设唯一正确答案。
- **对反思循环的影响**：自检优先用确定性规则校验（见 3.4-B），仅主观操作才用"多方案 + 人工选择"。

### 4A.2 文件范围定位机制（重点难题：Word「页」问题）

指令常指向"第 X 页 / 某段 / 某单元格"，但三种格式的可定位性差异极大：

| 格式 | 稳定的定位单位 | 不稳定/需特殊处理 |
| --- | --- | --- |
| **Word** | 段落序号、样式、标题层级、书签、章节 | **「页」不是文档固有属性**——分页由渲染引擎在排版时决定，`.docx` 本身不存页边界 |
| **PPT** | 幻灯片序号、形状/占位符 ID、元素类型 | 元素叠放层级、自由定位坐标 |
| **Excel** | 工作表名、单元格坐标（A1）、命名区域、行列号 | 合并单元格、跨表引用 |

- **Word「页」的处理策略**（必须在设计阶段解决）：
  1. 首选引导用户按**结构单位**定位（第 3 段、"引言"这一节、第二个表格），而非物理页；
  2. 若用户坚持按页，则依赖**预览渲染层的分页结果**做"页 → 段落范围"的映射，并明确告知"页码基于当前预览排版，导出后可能变化"；
  3. 兜底采用 LibreOffice 无头转换获得权威分页信息。
- **统一定位模型**：内部用「定位描述符（LocationSelector）」抽象所有范围（type + 参数），供工具调用与 diff 高亮共用。
- **风险提示**：范围定位不准会导致"改错地方"，是 P0 体验风险，需在原型阶段专项验证。

---

## 4B. 工具契约规范与插件化扩展（扩展性命脉）

> 产品的长期生命力取决于"能否低成本新增文件操作能力"。若每个新功能都要改核心代码，扩展性无从谈起。故将文件操作统一抽象为**工具（Tool）**，并规定统一契约。

### 4B.1 工具契约（Tool Contract）

每个文件操作能力封装为一个工具，遵循统一契约，以便：① 被 LLM 通过 Function Calling 调用；② 被自动化测试验收；③ 被第三方/后续扩展。

工具契约包含（概念级字段）：

| 字段 | 说明 |
| --- | --- |
| `name` | 唯一标识（如 `word.setFont`、`excel.createChart`） |
| `description` | 供 LLM 理解用途的自然语言说明 |
| `inputSchema` | 结构化入参（JSON Schema），含范围定位描述符 |
| `category` | 确定性 / 半确定性 / 生成性（见 4A.1） |
| `isDestructive` | 是否破坏性操作（决定是否强制确认，见 2A.1） |
| `preview` | 是否支持生成 diff 预览 |
| `execute` | 执行入口，返回变更集（ChangeSet）而非直接落盘 |
| `validate` | 确定性校验函数（用于自检，可选） |

- **统一返回「变更集（ChangeSet）」**：所有工具产出结构化变更（哪些位置、改前改后），供预览、部分接受、快照、diff 复用——这是打通 2A（信任流程）与 3.3（版本管理）的关键数据结构。

### 4B.2 插件化扩展

- **工具注册表（Tool Registry）**：工具以插件形式注册，核心调度层不硬编码具体能力。
- **扩展方式**：新增一种文件操作 = 实现一个符合契约的工具并注册，无需改动 Agent/UI 核心。
- **投标模块即插件**：需求 3.7 的投标处理作为一组专用工具插件，本期只需保证契约与注册机制就位（预留接口），后期独立开发。
- **未来兼容 MCP 等标准**：工具契约设计尽量向通用工具协议（如 Model Context Protocol）靠拢，为接入外部工具/服务预留空间。

---

## 5. 数据模型（概念级）

> v2.0 升级：引入**工作空间（Workspace）**组织多文件与会话；会话持久化以支持跨天续聊；版本存储明确"全量快照 vs 增量"策略；新增变更集（ChangeSet）打通信任流程与版本管理。

```
Workspace（工作空间 / 项目）
 ├─ id, name, createdAt
 ├─ files[]（归属的文件）
 ├─ conversations[]（归属的会话）
 └─ settings（默认模型、偏好作用域）

File（文件）
 ├─ id, workspaceId, name, type, path, size
 ├─ importedAt, modifiedAt
 ├─ pageCount / sheetCount
 ├─ tags[], thumbnail
 └─ currentVersionId

Version（版本快照）
 ├─ id, fileId, seq
 ├─ createdAt, triggerCommand, author（AI/用户）
 ├─ changeSummary
 ├─ storageType（full 全量 / diff 增量）
 ├─ snapshotPath（全量时）/ changeSetId（增量时）
 └─ parentVersionId（线性）
 // 存储策略：每 N 个版本落一次全量快照，其间存增量，兼顾恢复速度与磁盘占用

ChangeSet（变更集）★ v2.0 新增
 ├─ id, fileId, sourceCommand
 ├─ status（pending 待确认 / applied / discarded / partial）
 ├─ changes[]（location, before, after, intent, accepted:bool）
 └─ 供预览 diff、部分接受、生成快照复用

Conversation（会话）★ 持久化
 ├─ id, workspaceId, title
 ├─ createdAt, updatedAt
 └─ messages[]（role, content, toolCalls, changeSetId, resultCards）

UserPreference（用户偏好）
 ├─ id, scope（全局 / 工作空间 / 文件类型）
 ├─ rule（可读描述）, hitCount, weight
 └─ editable/deletable（用户可见可管理）

ModelConfig（模型配置）
 ├─ id, name, protocol（openai/anthropic）
 ├─ baseUrl, model, apiKey(加密)
 └─ isDefault

UsageRecord（用量记录）★ v2.0 新增
 └─ modelId, tokensIn, tokensOut, callCount, cost, timestamp

SearchIndex（检索索引）
 └─ fileId, contentTokens, metadata
```

---

## 6. 需求分析与完善建议

针对原始需求，以下为分析与增强建议：

### 6.1 建议补充的功能

1. **文件导出与格式转换**：处理后需支持导出为原格式，并建议支持导出 PDF。
2. **操作确认与预演**：AI 执行破坏性操作前先预览变更、用户确认再应用（降低误操作风险）。
3. **批量处理**：对多个文件套用同一指令（如"把这 10 个文档统一成公司模板"）。
4. **指令模板库 / 快捷指令**：沉淀常用指令，一键触发。
5. **本地大模型接入**：投标等涉密场景数据不出内网，建议预留 Ollama/vLLM 本地模型接入（属 OpenAI 兼容范畴，成本低）。
6. **成本与用量统计**：展示各模型的 token 消耗与调用次数，便于成本控制。
7. **错误恢复与自动保存**：处理中断时可恢复，防止工作丢失。
8. **文件对比引用**：对话中可引用多个文件进行对比或合并处理。
9. **导入进度与大文件处理策略**：大文件分块解析，避免界面卡顿。

### 6.2 对标 Claude Cowork 的功能补齐建议

- **工件（Artifact）化结果**：处理结果作为可独立预览、可迭代的工件卡片。
- **任务计划可视化**：复杂任务展示 AI 的执行步骤（plan → act → verify）。
- **上下文引用**：`@文件`、`@页码` 等方式精确引用操作对象。
- **可中断/可追问**：任务执行中可打断、可基于中间结果继续追问优化。

### 6.3 分期建议

分期以"可信可控"为主轴：先把信任交互与确定性操作的地基打牢，再叠加生成性与自进化能力。

- **一期（MVP）**：文件导入管理 + 三栏界面 + 基础对话处理（Word/Excel/PPT 的 P0 能力，见 3.2.5 验收标准）+ **信任交互流程**（第 2A 节：diff 预览 / 确认 / 部分接受，基于 ChangeSet）+ 快照回溯（第 3.3 节）+ 多模型配置。技术上以**确定性代码**（第 4A 节）覆盖 P0 操作，建立**工具契约**基线（第 4B.1 节）。同步落地核心 NFR：性能基线、隐私红线（数据流向透明、密钥安全）、操作原子性（第 7A 节）。
- **二期**：模板/风格匹配、PPT 美化、Excel 可视化增强、显式偏好记忆与有限自检（第 3.4 节 A/B）；引入半确定性/生成性操作的多方案与迭代；完善**范围定位机制**（第 4A.2 节，含 Word「页」映射与 LibreOffice 兜底）与可观测性（第 7A.4 节）。
- **三期**：投标文件专业模块（以插件形式接入 **Tool Registry**，第 4B.2 节）、批量处理、自动进化（附度量验证，第 3.4 节 C）、云端协作扩展与 MCP 兼容。

---

## 7. 风险与约束

| 风险 | 说明 | 应对 |
| --- | --- | --- |
| 开源库排版保真度 | 复杂 Word/PPT 排版用开源库处理与预览可能失真 | 关键场景引入 LibreOffice 无头转换兜底；明确能力边界 |
| PPT 处理能力有限 | 开源 PPT 库对复杂样式修改支持较弱 | 一期聚焦可行操作，复杂美化分期迭代 |
| 大模型输出不确定性 | AI 处理结果可能不符预期 | 快照回溯 + 操作预演 + 结果自检兜底 |
| 大文件性能 | 预览与解析大文件可能卡顿 | 分块处理、懒加载、异步渲染 |
| API Key 安全 | 涉及密钥存储 | 本地加密存储，绝不上传或明文记录 |
| 投标模块复杂度 | 需行业知识沉淀 | 本期仅预留接口，后续专项扩展 |

---

## 7A. 非功能性需求（NFR）

> 功能之外，以下质量属性直接决定产品是否"敢用、能用、可信"。尤其**隐私合规**——文档内容是否上传第三方模型，是办公场景的红线问题。

### 7A.1 性能

| 指标 | 目标 |
| --- | --- |
| 应用冷启动 | < 3s（普通配置桌面机） |
| 文件导入并渲染预览 | 常规文件（< 5MB）< 2s；大文件分块加载、界面不卡死 |
| 检索响应 | 万级以内文件本地检索 < 1s |
| 确定性操作执行 | 单文件常规操作 < 2s（不含 LLM 推理往返） |
| 大文件策略 | 超大文件（如 >50MB Excel）分块解析、懒加载、异步渲染，主线程不阻塞 |

### 7A.2 隐私与合规（红线）

- **数据流向透明**：明确告知用户"哪些内容会发送给大模型"。默认仅发送**必要的相关片段**（如被操作的段落/数据），而非整份文件。
- **本地优先**：文件、版本、索引、偏好、用量数据全部**本地存储**，不默认上云。
- **涉密场景**：为投标等敏感场景提供**本地模型接入（Ollama/vLLM）**选项，实现"数据不出内网"。
- **密钥安全**：API Key 本地加密存储，不明文落盘、不上传、不写入日志。
- **可清除**：用户可一键清除会话历史、缓存与本地数据。

### 7A.3 可靠性

- **崩溃恢复**：异常退出后可恢复未保存的工作与会话进度（自动保存草稿）。
- **操作原子性**：变更"要么完整应用、要么不应用"，不产生半改坏的文件；落盘前先写临时文件再替换。
- **数据不丢**：版本快照写入成功后才更新 currentVersionId；快照文件损坏可回退到上一个完好版本。
- **模型故障降级**：某模型不可用时提示并支持切换；网络中断有明确错误与重试。

### 7A.4 可观测性

- **操作日志**：记录指令、工具调用、变更集、结果（本地，用于排障与自检度量），不含敏感明文。
- **用量统计**：展示各模型 token 消耗、调用次数、成本估算（对应 UsageRecord）。
- **效果度量**：留存 3.4-C 定义的指令成功率、接受率等指标，供评估进化效果。

### 7A.5 可用性与兼容性

- **首次使用体验**：提供空状态引导（示例文件、快捷指令示例），降低上手门槛。
- **主题**：浅色/深色/自定义配色（对标 Cowork）。
- **操作系统**：macOS 优先，后续扩展 Windows，不排除后续基于源码迁移至其它 OS（见第 8 节）；LibreOffice 兜底方案打包先按 macOS 落地，依赖选型保持跨平台可迁移。

---

## 8. 已确认事项（原待确认，v2.1 关闭）

> 以下 7 项在 v2.0 中为待与需求方沟通事项，现均已确认，作为后续架构设计与开发的输入。

| # | 事项 | 确认结论 | 对设计的影响 |
| --- | --- | --- | --- |
| 1 | 部署形态 | 本地单机优先，预留云端扩展；确认后续存在团队共享文件需求，架构需为共享/协作预留扩展位 | 数据模型与存储层需支持后续从本地库平滑扩展到共享/云端；本期不实现协作，但接口与工作空间抽象需可扩展 |
| 2 | 投标模块范围 | 覆盖招标文件对点、废标项检查、应标目录、格式合规校验等；面向工程、政府采购、IT 服务三类招标 | 投标作为插件接入 Tool Registry（4B.2），三期专项开发；本期仅保证契约与注册机制就位 |
| 3 | 预览保真度 | 要求尽量真实还原，接受引入 LibreOffice 依赖以提升保真 | 预览与分页兜底采用 LibreOffice 无头转换（4A.2）；打包需内置/依赖 LibreOffice（先 macOS） |
| 4 | 操作系统范围 | macOS 优先；后续扩展 Windows；不排除后续基于源码迁移至其它 OS | 打包与 LibreOffice 兜底方案先按 macOS 落地，代码与依赖选型需保持跨平台可迁移性 |
| 5 | 数据规模 | 按上千个文件规模设计，同时预留后续海量文件支持能力 | 元信息检索需建索引（避免全表扫描）；存储与检索方案需为海量场景预留分页/分片/外部索引扩展 |
| 6 | PPT 生成能力 | 既支持"从零按大纲生成整套 PPT"，也支持对已有 PPT 做优化调整 | PPT 处理需新增"大纲→整套 PPT"的生成能力（生成性操作，见 4A.1），与既有优化能力并存 |
| 7 | 图像生成 | PPT/文档美化需具备 AI 生成配图能力 | LLM 适配层需扩展图像生成模型接入（文生图）；纳入多模型配置（3.5）与用量记录（5） |

---

## 9. 附录：需求覆盖对照表

| 原始需求 | 对应章节 | 状态 |
| --- | --- | --- |
| 1. 灵活导入 + 元信息 + 快速查找 | 3.1 | 已覆盖 |
| 2. 三栏界面（列表/聊天/预览） | 2, 3.1 | 已覆盖 |
| 3. 对话式处理（Word/PPT/Excel 各项） | 3.2、3.2.5（验收标准） | 已覆盖 |
| 3.7 投标文件处理 | 3.2.1 FW-7、4B.2（插件化接入） | 预留接口，后续扩展 |
| 3.8 模板/风格匹配 | 3.2.4 | 已覆盖 |
| 4. 多版本可追溯可恢复 | 3.3、5（ChangeSet/存储策略） | 已覆盖（快照+线性回溯） |
| 5. 界面参考 Claude Cowork + 可调配色 | 2, 6.2 | 已覆盖 |
| 6. 进化/反思/迭代优化 | 3.4 | 已覆盖（反馈学习+自检自优化） |
| 7. 多模型 API Key 配置 | 3.5 | 已覆盖（OpenAI 兼容+Anthropic） |

**v2.0 新增的支撑性内容（非原始需求，但为可用/可信所必需）：**

| 新增主题 | 对应章节 | 目的 |
| --- | --- | --- |
| 信任交互流程 | 2A | diff 预览 / 确认 / 部分接受 / 澄清追问 / 可中断 |
| 核心技术假设 | 4A | 确定性代码 vs LLM 操作分类；文件范围定位与 Word「页」问题 |
| 工具契约与插件化 | 4B | Tool Contract、Tool Registry、投标模块与 MCP 兼容 |
| 数据模型升级 | 5 | 工作空间、会话持久化、ChangeSet、版本存储策略、用量记录 |
| P0 功能验收标准 | 3.2.5 | 可验证的 Definition of Done |
| 非功能性需求 NFR | 7A | 性能 / 隐私合规红线 / 可靠性 / 可观测性 / 可用性 |

---

*文档结束。第 8 节待确认事项已于 v2.1 全部关闭，需求基线可进入架构设计与原型阶段。*
