# My-work-office 开发任务清单

> 版本：v1.0
> 日期：2026-09-10
> 状态：执行中
> 关联文档：《架构设计文档.md》（v1.2）、《需求文档.md》（PRD v2.1）、《后续工作计划.md》（v1.1）
> 本清单是可执行的开发任务分解，按阶段组织，每项任务含目标、验收标准、关联章节、阻断依赖。

---

## 阅读指引

- **优先级**：P0 = 必须完成且不可绕过；P1 = 本阶段必须完成；P2 = 本阶段完成但有降级空间；P3 = 远期/可延后。
- **状态**：`[ ]` 未开始 / `[~]` 进行中 / `[x]` 已完成 / `[!]` 阻断/有风险。
- **质量门禁**：每阶段末有门禁核查表，不通过不进入下一阶段。
- **纵切原则**：优先打通一条端到端链路（Word 确定性改写全链路），再横向扩 Excel/PPT 能力。

---

## 阶段 S0：高风险技术验证（PoC）

> 目标：在动一行产品代码之前，把架构中已知的技术黑盒逐一验证，输出"可行 / 有兜底 / 降级"明确结论。
> 阻断关系：S0 全部 P0 项通过 → 才能进入 S1 冻结 M1 → 才能进入 S2。

### T-S0-01【P0】python-pptx 打包全链路 PoC

- **目标**：验证 python-pptx + python-build-standalone 在 macOS 签名公证后可用，体积可接受，Windows 可行。
- **子任务**：
  - [x] 搭建最小脚本：读取一份真实 .pptx，修改文本/图片/形状属性，保存，PyInstaller --onedir 打包为独立可执行文件。结论：必须用 --onedir（onefile 每次解压 ~4.6s 不可接受）。
  - [x] 在 macOS 上通过 Node.js child_process 调用 onedir 产物，热路径平均 136ms，✅ PASS。
  - [ ] 执行正式 Developer ID 签名 + notarytool 公证，验证 GateKeeper 放行（S2 前置，需 Apple 证书环境）。
  - [x] 统计产物体积：onedir 33 MB（远低于 100 MB 上限），✅ PASS。
  - [ ] 在 Windows 10/11 执行相同流程，记录是否通过及差异点（S2 前置）。
- **验收标准（§3.4 五条）**：
  1. 能读取并原地修改真实 .pptx 中的文本、图片、形状属性，保存后 PowerPoint/WPS 可正常打开。
  2. 可执行文件在 macOS 签名公证后，GateKeeper 不拦截。
  3. 安装包体积增量 ≤ 100 MB（超出则触发裁剪或启动兜底评估）。
  4. child_process 调用延迟 ≤ 3 s（冷启动进程池预热后 ≤ 500 ms）。
  5. Windows 同等流程可复现（或有明确的差异清单与应对方案）。
- **兜底触发条件**：任意一条未通过 → 切换至 LibreOffice 宏路径，重新评估 PPT 改写架构。
- **关联**：架构设计文档 §3.4、§9.1

---

### T-S0-02【P0】LibreOffice 分发方式决策

- **目标**：确定"随包分发"还是"要求用户预装"，输出决策结论，为安装包体积基线和 NFR（§6）提供输入。
- **子任务**：
  - [x] 统计 LibreOffice 随包体积（macOS .app 打包后增量，参考值 ~300-500 MB）。**实测 804 MB**（Frameworks 435MB / config 107MB / help 61MB / extensions 61MB / fonts 51MB）。
  - [x] 评估 Homebrew `cask` 预装方案：安装器脚本可检测并提示安装，记录首次安装体验。**选定此方案**。
  - [x] 评估 LibreOffice Portable / 精简构建方案（去除不需要模块），记录能否缩减至 ≤ 150 MB。**无官方精简构建，社区 slim 仍 ~400-500MB，排除**。
  - [x] 调研用户群体（个人办公场景）预装 LibreOffice 的概率，判断预装方案的可接受度。**预装引导可接受**。
  - [x] 输出决策文档（一页），确认选定方案并更新 §3.4 与 §6 NFR 表中体积基线。**见 `poc/s0-02-libreoffice-dist/T-S0-02-决策文档.md`**。
- **验收标准**：有明确的"随包 / 预装 / 精简随包"选择及理由，体积基线数字写入架构文档。**✅ 选定「预装」，804MB 基线已写入架构文档 §3.4/§6**。
- **关联**：架构设计文档 §3.4、§6、§9.1

---

### T-S0-03【P0】Word「页」定位与分页保真 PoC

- **目标**：验证 LibreOffice 无头转换取权威分页方案可行，LocationSelector 的"页码"定位在真实文件上准确率达标。
- **子任务**：
  - [x] 用 `soffice --headless --convert-to pdf` 将 3 份不同复杂度的 .docx 转 PDF，提取页边界坐标。
  - [x] 将坐标映射回 docx 段落偏移量（via docx 库或 OOXML 解析），验证页码→段落映射准确率。**简单/表格/复杂文档均 100%**。
  - [x] 测试含分栏、浮动图、页眉页脚的复杂文档，记录映射失败场景与降级策略。**complex.docx（页眉页脚+图片占位）100%；分栏降级策略见结论报告**。
  - [x] 输出"可行 / 有兜底"结论：若准确率 ≥ 90% 则可行；否则降级为段落定位（放弃精确页定位）。**判定：可行，见 `poc/s0-03-word-pagination/T-S0-03-结论报告.md`**。
- **验收标准**：简单文档页码映射准确率 ≥ 95%；复杂文档有明确的降级说明。**✅ 简单 100%，自然分页 PASS，降级说明已记录**。
- **关联**：架构设计文档 §3.4（LibreOffice 进程池）、PRD 4A.2

---

### T-S0-04【P0】Word 段落级 diff PoC（diff 架构参照实现）

- **目标**：验证 Word 段落级内联 diff 在真实改写场景下可用，作为三格式 diff 架构的"参照实现"，后续 Excel/PPT 对齐。
- **子任务**：
  - [x] 实现最小 AtomicChange 序列化：给定 before/after 两份 docx 段落文本，输出 `AtomicChange[]`。
  - [x] 在渲染层实现段落级内联高亮（插入绿色/删除红色），无需完整 UI，用 HTML 文件验证即可。
  - [x] 验证部分接受逻辑：选择部分 AtomicChange 应用，其余跳过，结果正确。
  - [x] 验证回滚：应用后反向 diff 恢复原文，24/24 断言全通过（含 7 个边界用例）。
- **验收标准**：段落级 diff 渲染正确；部分接受后文档结构完整；回滚无数据丢失。
- **关联**：架构设计文档 §3.7

---

### T-S0-05【P1】LLM 工具调用可控性验证

- **目标**：验证 Agent 在工具调用链中，确定性工具不被 LLM 绕过，输出始终走 ChangeSet → 确认流程。
- **子任务**：
  - [x] 搭建 LLMGateway 最小实现（Mock ChatProvider 先行，OpenAI/Anthropic Key 后补）。
  - [x] 注册一个确定性 `ReplaceTextTool`，让 LLM 调用它修改文本，验证输出为 ChangeSet 而非直接改文件。
  - [x] 测试 LLM 尝试"跳过确认"的提示词注入，验证系统层面强制拦截。
  - [x] 验证工具调用失败时的降级：工具抛错 → Agent 返回错误 ChangeSet → UI 可展示错误，不崩溃。
- **验收标准**：所有工具调用均产生 ChangeSet，无静默改文件路径；工具失败有可感知错误提示。
- **关联**：架构设计文档 §3.1、§3.2、§3.3；PRD 2A、4A.1

---

### T-S0-06【P1】AI 生成配图可用性验证

- **目标**：验证图像生成 Provider（文生图接口）在 Electron 环境中可正常调用，图片可插入 docx/pptx。
- **子任务**：
  - [ ] 调通至少一个文生图接口（如 OpenAI DALL-E 3 或本地 Stable Diffusion）。
  - [ ] 将返回图片数据写入 .docx 的 InlineImage，用 docx 库验证可打开。
  - [ ] 验证图片数据流不落盘明文（隐私红线：写入文档前不保存至临时目录或日志）。
- **验收标准**：配图可生成并插入文档；无图片数据明文落盘。
- **关联**：PRD 第 8 节⑦；架构设计文档 §3.5

---

### ✅ S0 质量门禁

| 门禁项 | 标准 | 状态 |
| --- | --- | --- |
| T-S0-01 python-pptx PoC | 5 条验收标准全通过，或有明确兜底路径 | `[x]` |
| T-S0-02 LibreOffice 分发决策 | 有选定方案 + 体积基线数字 | `[x]` |
| T-S0-03 Word 分页保真 | 简单文档准确率 ≥ 95% | `[x]` |
| T-S0-04 Word diff PoC | 段落级 diff 渲染正确 + 部分接受 + 回滚 | `[x]` |
| T-S0-05 LLM 可控性 | 无静默改文件路径 | `[ ]` |
| T-S0-06 配图可用性 | 可生成并插入，无明文落盘 | `[ ]` |
| 技术可行性报告 | 输出文档，每项有"可行/有兜底/降级"结论 | `[ ]` |

> **全部通过后**：更新架构设计文档至 v1.3（纳入 PoC 结论），状态改为"已冻结基线"，进入 S1 冻结 M1。

---

## 阶段 S1 补充：M1 冻结前置任务

> S0 通过后，在正式开始 S2 编码前，完成以下设计细化与评审。

### T-S1-01【P0】架构文档评审与 M1 冻结 ✅（2026-09-11 完成）

- [x] 组织架构评审（可自评），核对 §9.1 清单全部有结论。（§9.1 新增「当前状态」列，各项标注 ✅已验证/已决策 或 ⏳挂账）
- [x] 将 S0 PoC 结论纳入架构文档 v1.3（更新 §3.4 PPT 路径选定、§3.4 LibreOffice 分发方案、§6 NFR 体积基线）。（v1.3 修订说明 + §9.1 回灌 T-S0-01~05 结论）
- [x] 确认数据模型（§4 SQLite 表结构）为可执行基线，Drizzle schema 文件就绪。（`app/src/main/db/schema.ts` 9 表；`npm run db:generate` 成功生成迁移 `0000_bitter_vengeance.sql`）
- [x] 确认工具契约（§3.3 Tool interface）版本冻结，后续变更需版本化。（§3.3/§4 加 🔒M1 冻结标注；文档状态改「已冻结基线」）
- **验收标准**：架构文档状态改为"已冻结基线"；Drizzle schema 文件存在且可运行迁移。✅ 均达成（typecheck:node 零错误；drizzle-kit generate 识别 9 表并产出迁移）

---

### T-S1-02【P1】UI/UX 详细设计 ✅（2026-09-14 收口）

- [x] 完成三栏布局高保真设计稿（文件列表 / 对话 / 预览/属性面板）。（规格稿 `ui-design/index.html` §01；另交付级单屏稿 `ui-design/main-screen.html`，Cowork 式三栏 + 右栏上下文 rail + 实时预览）
- [x] 设计 diff 预览交互：Word 内联高亮 / Excel 单元格着色 / PPT 整页并排（S2 分级）。（§02 diff 三格式，对齐架构 §3.7；主界面含对照模式 原文↔应用后）
- [x] 设计信任交互流程（2A）：ChangeSet 卡片 / 部分接受勾选 / 确认/拒绝按钮 / 回退入口。（§03 信任交互 2A + §07 三档信任模式）
- [x] 设计多模型配置页（API Key 录入 / 模型切换 / 用量展示）。（§04 多模型配置）
- **验收标准**：设计稿覆盖全部 P0 交互路径（PRD 3.2.5）；有暗色/亮色两套方案。✅ 均达成
  - 覆盖范围：`index.html` 共 11 段规格（01 三栏 / 02 diff 三格式 / 03 信任交互 / 04 多模型 / 05 操作能力矩阵 / 06 执行轨迹 / 07 三档信任 / 08 跨文件+Skills / 09 版本追溯 / 10 知识沉淀 / 11 布局可调节性），覆盖 PRD 3.2.5 全部 P0 路径。
  - 双主题：OKLCH token 体系 + `[data-theme="light"]` 全量覆盖，暗/亮两套方案齐备，一键切换。
  - 评审加固：设计评审 🔴🟡 项已分别写入架构 §7.1/§7.2 与任务补录（T-S2-08、T-S3-12）。
- **后续可调整性（保障）**：见下方「UI 可维护性契约」，确保后续调整低成本。
- **关联**：PRD §3.2.5、§3.3；架构设计文档 §7.1、§7.2

#### UI 可维护性契约（后续调整优化保障）

> 目的：确保本 UI 交付方案在 S2 及以后**可低成本调整**，而非推倒重来。

| 调整类型 | 便捷机制 | 落地位置 |
| --- | --- | --- |
| 改视觉（颜色/圆角/阴影/字体） | 单一 `:root` token 体系，改 token 即全局生效；暗/亮双主题仅覆盖同名 token | `main-screen.html` `:root` / `[data-theme="light"]`；S2 落 Tailwind theme |
| 改布局尺寸 | CSS 变量驱动（`--w-left`/`--w-right`/`--composer-h`），限位集中定义 | 架构 §7.1 `UiState.layout` |
| 改视图/交互逻辑 | 状态驱动（Zustand `UiState`），视图切换改 state 而非改 DOM | 架构 §7.1 状态模型草图 |
| 加文件类型（PDF/MD/图片） | `previewerRegistry[fileType]` 注册即用，切换逻辑零改动 | 架构 §7.1；T-S3-12 |
| 加 Skill/连接器 | 能力面板数据驱动，新增不改 UI 代码 | 架构 §7.1；T-S3-12 |
| 适配不同窗口 | 断点自动收敛，不写死宽度 | 架构 §7.2 |
| 组件实现 | Tailwind + shadcn/ui（Radix），设计 token 直接映射；规格稿为唯一视觉基准 | T-S2-01、T-S2-08 |

**约束**：以上 token 取值、`UiState` 形状、断点数值为 S1 冻结契约；后续若需变更，走版本化修订（记录到本清单与架构文档），避免实现与设计漂移。

---

## 阶段 S2：工程骨架与最小可信闭环

> 目标：搭建 Monorepo 骨架，打通"导入→对话→ChangeSet→diff→确认→快照→回溯"一条完整链路（Word 文件，确定性操作）。
> 纵切原则：先 Word 全链路，不要同时铺 Excel/PPT。

### T-S2-01【P0】Monorepo 工程骨架

- [x] 初始化 `electron-vite` + `electron-builder` Monorepo，目录对齐架构文档 §8。（证据：`app/electron.vite.config.ts` 三进程配置；`npm run build` 产出 out/main、out/preload、out/renderer）
- [x] 配置三进程边界：`main/`（主进程）、`preload/`（contextBridge 白名单）、`renderer/`（React 18 + TypeScript 5）。（证据：`src/preload/index.ts` 经 `contextBridge.exposeInMainWorld('api', …)` 仅暴露 `ping` 白名单；E2E `smoke.spec.ts` 验证 `window.api.ping()===\`pong\``）
  - 说明：`packages/utility/`（Utility Worker）随 T-S2-02 数据层落地，本项不含。
- [x] 集成 Tailwind CSS + shadcn/ui（Radix），配置设计 token（颜色 / 字体 / 间距 / 圆角）。（证据：`src/renderer/src/assets/main.css` 24 个 OKLCH token × dark/light；`tailwind.config.js` token 映射；`components.json` + `components/ui/button.tsx`）
- [x] 配置 Zustand + TanStack Query，验证渲染层 IPC 数据拉取基本可用。（证据：`store/ui-store.ts`（persist）；`lib/query-client.ts` + `providers/AppProviders.tsx`；`App.tsx` 状态驱动；E2E 验证 IPC 通道可用）
- [x] 配置 Vitest（单元）+ Playwright（E2E），空测试套件可运行。（证据：`vitest run` 14 用例通过；`playwright.config.ts` + `e2e/smoke.spec.ts` 3 用例通过 `npm run test:e2e`）
- [x] 配置 pino 日志（主进程/Utility），确认不输出密钥或文档内容。（证据：`src/main/logger.ts` redact 覆盖 apiKey/token/content/text/prompt/messages 等；`logger.test.ts` 断言敏感字段被 `[REDACTED]` 且明文不落盘；gateway 仅记 token 计数不记正文）
- [x] 配置 ESLint + TypeScript strict 模式，CI lint 通过。（证据：`eslint.config.js` 采用 `tseslint.configs.strict`；`npm run lint` 通过；`npm run typecheck` 双 project 通过）
- **验收标准**：`npm run dev` 启动 Electron 窗口无报错；`npm run test` 空套件通过；`npm run lint` 通过。✅ 达成（五道门禁 typecheck/lint/unit/build/e2e 全绿；修复了 preload 产物为 `.mjs` 而主进程加载 `.js` 的启动缺口，见 `src/main/index.ts`）
- **关联**：架构设计文档 §8

---

### T-S2-02【P0】SQLite + Drizzle 数据层

- [x] 集成 `better-sqlite3`，确认在 Utility 进程（Worker Thread）中独占运行，主进程通过 MessagePort 调用。（证据：`src/main/db/worker.ts` 独占 `openDatabase` + `DataService`，主进程经 `src/main/db/client.ts` 的 `{id,type,payload}` 协议调用；`src/main/index.ts` 启动 `initDataLayer()`。**实现说明**：实作采用 Electron `utilityProcess.fork` 而非 `worker_threads.Worker`——后者在 Electron 运行时下加载 `better-sqlite3` 原生模块会崩溃退出整个进程（E2E 实测），`utilityProcess` 为 Node ABI 进程、原生模块正常加载，且仍属 §2 允许的「Utility 进程」范畴，协议不变）
- [x] 用 Drizzle ORM 实现 §4 全部表（**命名以 §4 权威表名为准**）：`workspaces`、`files`、`conversations`、`messages`、`change_sets`、`versions`、`model_configs`、`fts_files`（FTS5）。（证据：`src/main/db/schema.ts` 10 张表 + `migrations/0000_slippery_proemial_gods.sql`；任务清单概述别名 `snapshots`/`llm_providers`/`fts_index` 已按 §4 统一为 `versions`/`model_configs`/`fts_files`）
- [x] 实现 `SearchRepository` 抽象（`SqliteFtsSearchRepository`），FTS5 全文检索可用。（证据：`src/main/db/search-repository.ts` 接口 + FTS5 实现（`snippet()`/`rank`），业务层只依赖接口，可替换为 Orama/外部索引）
- [x] 实现启动时 pending ChangeSet 扫描与孤立 `.tmp` / 外置 changes 文件清理（崩溃恢复路径，§5）。（证据：`src/main/db/recovery.ts` + `data-service.ts`；启动扫描 pending 不自动应用、清理孤立 `.tmp`、外置 `.changeset` 按「先删文件后删行」清理；`discardChangeSet` 同样先删文件再置 discarded）
- [x] 编写单元测试：CRUD 操作 / FTS5 检索 / pending ChangeSet 扫描 / changes_path 超阈值写外置文件。（证据：`src/main/db/data-service.test.ts` 7 用例——迁移建表含 `fts_files`、File CRUD、FTS5 命中/删除、pending 扫描不自动应用、孤立外置文件清理、小变更内联、超 512 KB 外置往返）
- **验收标准**：所有表迁移可执行 ✅（`0000` 迁移 10 表 + FTS5 虚表，单测断言建表）；FTS5 检索返回正确结果 ✅（命中/移除断言）；崩溃恢复逻辑有单元测试覆盖 ✅（pending 扫描 + 孤立 .tmp/.changeset 清理）。五道门禁 typecheck/lint/unit(21)/build/e2e(3) 全绿。
- **关联**：架构设计文档 §3.6、§4、§5；跨进程协议 `src/shared/db-protocol.ts`

---

### T-S2-03【P0】文件导入与元信息提取（Word）

- [x] 实现文件选择对话框（IPC → 主进程 `dialog.showOpenDialog`）。（证据：`src/main/index.ts` `file:importWord` handler 调 `dialog.showOpenDialog`，多选 .docx/.doc，逐文件 try/catch 汇总 `{canceled, imported, failures}`；preload 白名单 `importWord` + `src/shared/ipc.ts` 类型）
- [x] 用 `mammoth` 提取 Word 文档文本内容与基础元信息（标题、字数、修改时间）。（证据：`src/main/files/word-parser.ts` `parseWordFile` 用 `mammoth.extractRawText` 抽正文；`countWords`（CJK 字符 + 拉丁分词）计字数；`pickFirstParagraph` 启发式取标题；修改时间取 `statSync().mtimeMs`。files 表无 title/wordCount 列（§4），故字数/标题写入 FTS5 metadata，不做 schema 漂移；page_count 需 LibreOffice，S2 未集成 → 诚实置 null 并注释）
- [x] 写入 `files` 表，触发 FTS5 索引更新。（证据：`src/main/import/import-service.ts` `importWordFile` 编排：解析 → `file.create` 写 files 表 → `search.indexFile` 写 FTS5，metadata=`[name, heading, words:N]`；FTS5 由 `unicode61` 切换为 `trigram` 分词器以支持中文/子串检索，见 `schema.ts` + 迁移 `0000`）
- [x] 实现原子写入：`写.tmp → fsync → rename`（§5）。（证据：`src/main/db/atomic-write.ts` `atomicWriteFileSync` 扩展支持 `string | Buffer`；导入时以原子写把源文件副本落盘到 userData/files）
- [x] 编写集成测试：导入 3 份不同复杂度的真实 .docx，验证元信息正确、索引可检索。（证据：`src/main/import/import-service.test.ts` 5 用例——单段/多段（标题抽取）/中文+特殊字符（FTS5 经「营业收入」检索命中）/同工作区列表 2 文件/非法类型拒绝；.docx 夹具由 `src/main/files/__fixtures__/make-docx.ts` 基于 JSZip 真实生成）
- **验收标准**：导入后文件列表可展示 ✅（`file:list` IPC + `listFiles` 桥接）；元信息准确 ✅（字数/标题/mtime 断言）；FTS5 可按关键词检索到导入文件 ✅（中文 trigram 命中断言）。五道门禁 typecheck/lint/unit(27)/build/e2e(4，含数据层就绪断言，连跑两次幂等) 全绿。
- **架构隔离**：文件引擎独立 Utility 进程（`src/main/files/worker.ts` + `client.ts`），与 DB Utility 进程分离，避免 CPU 密集解析阻塞 SQLite 写库（架构 §2、§3.4）。
- **重要修复 1（T-S2-02 遗留隐患根除）**：新增的数据层 e2e 断言暴露 better-sqlite3 在 Electron 下 `new Database()` SIGSEGV。根因：v13 用 N-API 10 编译，Electron 33 内置 Node 20 仅 N-API 9。修复：锁定 `better-sqlite3@12.11.1`（经典 per-ABI 绑定）+ `@electron/rebuild` 精确匹配 Electron modules=130；双 ABI 共存方案见 `src/main/db/connection.ts` `resolveNativeBinding()` 与 `vendor/better-sqlite3/README.md`（Electron 运行时用 build/Release，vitest 经 nativeBinding 用 Node-ABI 副本）。
- **重要修复 2（迁移幂等）**：迁移 SQL 为裸 `CREATE TABLE`，原实现每次启动全量重放，二次启动因“表已存在”致 `initDataLayer` 静默失败（e2e 首跑通、二跑超时）。修复：`connection.ts` 引入 `__mwo_migrations` 记账表 + 基线检测，迁移“恰好执行一次”，单事务原子提交；回归测试 `data-service.test.ts` “re-opening the same db file … (idempotent)”。
- **关联**：架构设计文档 §3.4、§4、§5；PRD 3.1

---

### T-S2-04【P0】LLM 适配层 + 基础工具调用

- [x] 实现 `LLMGateway`：支持 OpenAI 兼容协议，流式输出。（证据：`src/main/llm/gateway.ts` 重试（maxRetries）+ 超时（Promise.race）+ 用量计量注入，`chatStream` 转发 Provider 的异步流式块；`gateway.test.ts` 5 用例——成功透传 toolCalls/流式 chunk、重试后成功、重试耗尽抛错含次数、超时中止、usage 记账）
- [x] 实现 `ChatProvider`（OpenAI 兼容）与 `UsageMeter`（token 计数）。（证据：`src/main/llm/openai-provider.ts` SSE 流式解析 + tool_calls 增量聚合 + 协议归一化（单 assistant 消息 toolCalls 数组、`tool.tool_call_id`、tools 包裹 `{type:'function'}`、JSON.stringify arguments，§3.5），`openai-provider.test.ts` 7 用例；`usage-meter.ts` 优先用 Provider 精确 usage、缺失时 CJK 启发式估算（中文字符≈1 token + 拉丁分词），`usage-meter.test.ts` 8 用例；`mock-provider.ts` 无 MWO_LLM_* 环境变量时诚实降级（场景：含"替换"意图→引号正则抽 find/replacement→目标段落 0；工具结果→固定收尾语）；`provider-factory.ts` `resolveChatProvider()` 按 env 装配真实/Mock 并回报 mode/note）
- [x] 实现第一个确定性工具 `ReplaceTextTool`：接收文本替换指令，输出 `ChangeSet`（不直接改文件）。（证据：`src/main/tools/replace-text-tool.ts` 校验段落存在/查找文本存在，多匹配全部替换（split/join）；`LOCATION_NOT_FOUND`/`TEXT_NOT_FOUND` 均为 recoverable 错误；`contextHint` 返回 `{strategy:'paragraph-exact'}`；`validate` 拒绝非法变更类型/缺 after.text；`replace-text-tool.test.ts` 8 用例——pending ChangeSet 结构、多匹配、两类错误、documentId 未绑定、contextHint、validate 正反）
- [x] 实现 Tool Registry：注册 `ReplaceTextTool`，LLM 可通过 function calling 调用。（证据：`src/main/tools/registry.ts` `toSchemas()` 用 zod v4 原生 `toJSONSchema(schema, {io:'input'})` 导出 JSON Schema，不可表示类型 catch 降级 `{}`（`zodToJsonSchemaSafe`）；`registry.test.ts` 4 用例含 zod v4 回归守卫（replaceText 参数含 location/find/required）与 `z.bigint()` 降级路径——zod v4 对不可表示 schema 默认 throw，正是降级分支的确定性触发器）
- [x] ContextBuilder 基础实现：截取当前文件的相关段落（隐私红线：仅发送必要片段），通过 `contextHint` 覆盖点预留。（证据：`src/main/context/context-builder.ts` 双预算 `DEFAULT_CONTEXT_BUDGET {maxParagraphs:6, maxParagraphChars:500, maxTotalChars:2000}`；查询词评分（CJK bigram + 西文词）选段、无命中回退头部；`pinnedIndexes` 经 contextHint 优先锁定（§3.3 覆盖点落地）；超总预算整段丢弃绝不半截；单段超长截断标注 `……[段落已截断，原文 N 字]`；审计元数据 selected/omittedCount/truncated/sentChars/totalDocChars；`context-builder.test.ts` 7 用例——词选段排除无关段、头部回退、pinned 锁定、单段截断、总预算、空文档、50 段大文档审计（sentChars < totalDocChars））
- [x] 编写单元测试：工具调用返回 ChangeSet；ContextBuilder 截断行为；UsageMeter 计数。（证据：单测 71/71（12 文件，新增 44：replace-text-tool 8 + registry 4 + context-builder 7 + agent-service 5 + usage-meter 8 + openai-provider 7 + gateway 5）；`agent-service.test.ts` 5 用例覆盖完整 runTurn 编排——真实 DataService/openDatabase/makeDocx 夹具导入→替换→磁盘未变断言、FILE_NOT_FOUND/UNSUPPORTED_FILE_TYPE/WORD_PARSE_FAILED、parseCount 验证 DocumentSession contentHash 缓存（第二轮不重解析）；主进程 `chat:send` handler + preload `chatSend` 桥接 e2e 第 5 条断言未知文件返回结构化 FILE_NOT_FOUND）
- **验收标准**：LLM 调用 `ReplaceTextTool` 后返回 ChangeSet，不静默改文件 ✅（`agent-service.test.ts`"一句话替换"：changeSets[0] 为 pending replaceText 且 before/after 精确匹配，同时 `parseWordFile` 重新读盘断言仍含原文"缓慢"、不含"强劲"——磁盘未被动过）；ContextBuilder 不发送完整文件内容 ✅（`context-builder.test.ts`：sentChars ≤ maxTotalChars，50 段大文档 sentChars < totalDocChars 且 omittedCount = 50 - selected.length，`[段落N]` 标注数 === selected.length）。五道门禁 typecheck/lint/unit(71)/build(34.8kB)/e2e(5) 全绿。
- **关联**：架构设计文档 §3.1、§3.2、§3.3、§3.5；PRD 2A、4A.1

---

### T-S2-05【P0】ChangeSet 持久化 + diff 预览 + 信任交互

- [x] ChangeSet 写入 `change_sets` 表（含 changes_path 外置文件策略：超 512KB 写外置文件，§4）。（证据：`src/main/trust/trust-service.ts` `persistPending`（supersede：同文件旧 pending 先 discard 再落新行，§5 单文件单 pending）由 `AgentService.runTurn` 逐 ChangeSet 调用（trust 可选依赖、落库失败仅告警不中断回合）；`db-protocol.ts` 扩展 `changeSet.getResolved/listPendingResolved/discard` + `file.update`；集成适配器 `changeSet.create → DataService.createChangeSet`（唯一实现 >512KB 外置化，直连 repository 会绕过 §4 策略）；`trust-service.test.ts` 全链路 / 外置 513KB / supersede 用例）
- [x] 实现 Word 段落级 diff 渲染组件（基于 T-S0-04 PoC 结论，内联插入/删除高亮）。（证据：`src/renderer/src/lib/inline-diff.ts` 词级 LCS——4 类 token 切分（CJK / 拉丁数字 / 空白 / 标点）、`MAX_DP_CELLS=40_000` 超限降级整段 del+add、回溯删优先 + 相邻同类型合并、往返还原不变量（equal+del 拼 before / equal+add 拼 after）；`inline-diff.test.ts` 15 用例——中文「缓慢→强劲」/ 英文 quick→slow / 混排 v1→v2 词级精确断言 + 9 对中英混排往返矩阵 + 降级（250×250 CJK）+ 碎片化）
- [x] 实现信任交互 UI：ChangeSet 卡片 / 全部接受 / 全部拒绝 / 部分接受勾选。（证据：`src/renderer/src/components/chat/ChangeSetCard.tsx`——默认全选（全部接受为默认路径，§5）；勾选集合即 `acceptChangeset(id, ids)` 入参，「应用变更」全选时不传 ids 走 TrustService 全部接受口径、「部分应用」仅部分勾选时可用、「放弃」走 `rejectChangeset`；`isApplicable` 与 `TrustService.accept` 同判定口径（paragraph 位置 text 变更），不提供无法履行的勾选（显示「暂不支持应用」）；`App.tsx` 会话区接入 + preload 三桥接 `listPendingChangesets/acceptChangeset/rejectChangeset`）
- [x] 确认后原子写入 docx 文件（`写.tmp → fsync → rename`）；拒绝后清理 ChangeSet 与外置文件（先删文件后删 DB 行，§5）。（证据：`TrustService.accept` 六步流——`getResolved` → `word.parse` 基线比对（不一致抛 `BASELINE_MISMATCH` 保持 pending，§2A.6）→ edit 映射（仅 paragraph+text）→ `word.applyParagraphEdits`（word-writer 内走 atomic-write `.tmp→fsync→rename`）→ `file.update` 用写后重解析哈希刷新基线 → `updateStatus` applied/partial；reject 走 `discardChangeSet`（safeUnlink 外置文件在前、置 discarded 在后）；`trust-service.test.ts` 外置用例 reject 后外置文件删除、supersede 旧外置清理、BASELINE_MISMATCH 用例）
- [x] 编写集成测试：完整链路 ChangeSet 写入→diff 渲染→全部接受→文件变更→全部拒绝→文件不变。（证据：`trust-service.test.ts` 7 用例——①全链路（runTurn 落库→listPendingViews→全部接受→磁盘重解析断言新文本 / contentHash 一致 / applied / 无 .tmp）②部分接受（3 变更勾 2，未勾段落原样、paragraphCount=3、partial）③拒绝（readFileSync 字节比对不变 + discarded）④外置 >512KB 往返（resolve 回读完整）⑤supersede ⑥BASELINE_MISMATCH ⑦错误分支（CHANGESET_NOT_FOUND / TRUST_NO_SELECTION / UNSUPPORTED_CHANGE_TYPE）；`e2e/smoke.spec.ts` 三通道——listPending 返回数组、accept/reject 未知 id 结构化 CHANGESET_NOT_FOUND）
- **验收标准**：diff 渲染正确（插入绿/删除红）✅（`inline-diff.test.ts` 中文/英文/混排词级精确断言 + 往返不变量矩阵；`ChangeSetCard.tsx` del=红底删除线 `bg-red-soft text-red line-through`、add=绿底 `bg-green-soft text-green`）；部分接受后文档结构完整 ✅（部分接受用例：3 段文档勾 2，重解析 paragraphCount=3、未勾段落字节原样）；拒绝后无残留文件 ✅（拒绝用例 readFileSync 字节级比对 + 目录无 .tmp 残留；外置用例 reject 后 .changeset 删除）；外置 changes 文件清理一致性有测试覆盖 ✅（reject / supersede 双路径外置删除断言，附录 §9.1 对应行同步翻绿）。五道门禁 typecheck / lint / unit(103 用例 15 文件) / build / e2e(8) 全绿。
- **关联**：架构设计文档 §3.7、§4、§5；PRD 2A

---

### T-S2-05A【P1】手动优化反馈闭环 · 第 1/2 层（对应 PRD 2A.6）

> 场景：Agent 调整不达预期时，用户自己动手改到满意；系统捕获该信号（第 3 层回灌偏好/知识放 S3，见 T-S3-13）。

- [x] **第 1 层 · 应用内手动微调捕获**：预览/编辑区用户手动调整 → 生成 `ChangeSet(source=manual)` 记录 Agent 版→用户版 diff，走标准信任流程落盘 + `Version(author=user)` 快照。（证据：`trust-service.ts` createManualPending——基线四重校验（FILE_NOT_FOUND/BASELINE_MISMATCH/MANUAL_PARAGRAPH_COUNT_MISMATCH/MANUAL_PARAGRAPH_INVALID）→ `word.parseParagraphs` 重解析现盘段落 → paragraph-diff 逐段比对 → changeSet.create（source=manual、toolName=manualEdit、renderHint=inline）→ supersede 同文件旧 pending → accept 走标准信任流程（word.applyParagraphEdits + version.onApplied source='manual'）；渲染层 `ManualEditPanel.tsx` 右栏段落编辑，preload 桥接 `file:getParagraphs`/`manual:createChangeset` 两通道）
- [x] **第 2 层 · 外部编辑感知**：基于 `files.content_hash` + `modified_at`，通过 fs watcher / 启动扫描检测应用外改动。（证据：`external-watch.ts` ExternalWatchService——启动即 `file.listAll` 跨工作区全量扫描 + fs.watch 目录监听去抖重扫（WATCH_DEBOUNCE_MS=1000）+ mtime 预筛（MTIME_SLACK_MS=2000 仅豁免应用自身写入余量）+ contentHash 字节级权威比对；哈希一致静默重定基线不误报（自写不检出红线有测试），不一致进 detections 待处理）
- [x] **外部改动处理 UI**：不静默覆盖，提供「采纳为新基线（`source=external`）/ 忽略 / 查看 diff」三选项。（证据：`ExternalChangePanel.tsx` 三选项横幅 + getExternalDiff 段落级 before/after diff 视图；采纳走 acceptExternal（changeSet source=external + version.onApplied source='external'），忽略走 ignoreExternal（仅重定基线、currentVersionId 保持 null）——均为用户显式触发，检出本身不改任何文件）
- [x] **冲突消解**：存在 `pending` ChangeSet 且基线失效时，将 pending 置 `stale` 并要求用户显式处理（不静默套用）。（证据：`external-watch.ts` detectOne 检出即 `changeSet.updateStatus(pending→stale)` + `countByFileStatus` 统计 stalePendingCount 随检出视图下发；`trust-service.ts` accept 对 status!=='pending' 一律拒绝 CHANGESET_NOT_PENDING——stale pending 无法被套用，须走外部改动三选项或重新微调）
- [x] `change_sets.source` 字段落地（ai/manual/external）+ `versions.author`（ai/user/external）。（证据：ChangeSetRecord.source 贯穿创建/视图（ChangeSetView.source 渲染层来源徽标）；VersionService.onApplied 映射 manual→'user'、external→'external'、ai→'ai'，changeSummary 分别为「手动修改 x/y 项」「外部编辑 x/y 项」「AI 修改 x/y 项」；db-protocol 增 file.listAll/countByFileStatus/listPendingResolved 路由支撑）
- [x] 编写测试：手动微调生成 manual ChangeSet；外部改文件被检出并提示；stale pending 正确标记。（证据：`trust-service.test.ts` 新增 6 用例共 13——①手动微调全链路（manual 视图 → accept → v1 author 'user'/「手动修改 1/1 项」→ 二轮 v2 → restore 回 v1 字节级还原）②无差异返回 null 不落库 ③段落数/索引/文件校验且文件字节不变 ④空段含换行拒绝 ⑤supersede 顶掉同文件 AI pending ⑥磁盘被覆盖后 BASELINE_MISMATCH；新建 `external-watch.test.ts` 5 用例——①检出（自写不误报 → 外部编辑后 1 条 + 关联 pending 置 stale + stalePendingCount:1 + diff before/after 断言 + stale accept 拒绝）②采纳为新基线（source external/author 'external'「外部编辑 1/1 项」/快照=磁盘/重复操作拒绝）③忽略（重定基线、无版本、raw SQL change_sets 0 行、随后手动微调可用——第 1/2 层闭环）④无版本基线采纳（EXTERNAL_DIFF_NO_BASELINE → 采纳产生 v1）⑤哈希一致静默重定基线；`smoke.spec.ts` +3 通道冒烟（file:getParagraphs / manual:createChangeset / external:accept 未知文件结构化 FILE_NOT_FOUND））
- **验收标准**：三种来源变更均可追溯（谁改的一目了然）；外部改动绝不静默覆盖；基线失效的 pending 必须显式处理；手动优化同样进版本快照可回退。✔ 已验证（五道门禁全绿：typecheck node+web / lint / vitest 18 文件 129 测试（原 118 + 6 manual + 5 external-watch） / build / e2e 13 用例含三条新通道冒烟）
- **关联**：架构设计文档 §5.1、§4；PRD 2A.6

---

### T-S2-06【P0】快照与线性回溯

> 进展（本轮）：T-S0-04 PoC diff 引擎正式移植 `app/src/shared/paragraph-diff.ts`（锚定 before 序列 / 按锚分组从后往前重放 / 回滚=反向重 diff），后像快照 + VersionService + 回溯 UI 全链路落地。物理表沿用架构 §4 M1 冻结的 `versions`（本清单原文"snapshots"以架构为准）；快照时点为 §5 冻结的「确认（全部接受）后」后像。

- [x] 实现快照写入：每次 ChangeSet 确认（全部接受）后自动创建 `versions` 记录（存文件 hash + 时间戳）。（证据：`version-service.ts` onApplied 钩子——file.copy 原子复制工作文件至 `snapshots/{versionId}.snapshot.docx` → `versions` 行 createdAt 时间戳 + seq 递增，文件 hash 由 parse 重算回填 `files.content_hash`、快照完整性由字节级快照文件保证 → 快照与行落库成功后才移 currentVersionId 指针（§5 7A.3））
- [x] 实现回溯 UI：对话面板显示版本历史，点击某版本可预览 diff 并选择恢复。（证据：`components/chat/VersionHistoryPanel.tsx`——listVersions 时间线 + getVersionDiff 差异预览 + restoreVersion 恢复；preload 白名单三通道 `version:list/diff/restore`）
- [x] 回溯时原子替换文件（`写.tmp → fsync → rename`），新建一条反向 ChangeSet 记录。（证据：`version-service.ts` restore()——ALREADY_CURRENT 守卫 → 丢弃同文件 pending → copyFileAtomicSync 快照→工作文件（atomic-write.ts `.tmp`+fsync+rename）→ 反向 ChangeSet 出生即 applied（source=manual、恢复到版本 v{seq}）→ 回溯自身后像快照独立文件（不与目标共享，孤儿清理按 versions 引用判定）→ currentVersionId/contentHash 回填；先替换后记账——崩溃窗口仅"文件已恢复无记录"可重试补救，无"记录已写文件未换"的谎报漏洞）
- [x] 编写集成测试：导入→修改→快照→再修改→回溯，验证文件内容正确恢复。（证据：`version-service.test.ts` 5 测试——①全链路含二次回溯（恢复后文件 byte 级等于 v1 快照、反向 ChangeSet applied、回溯自身产生 v3/v4 可再回溯）②diff 预览：回溯前 1 条 text 变更、回溯后归零 ③错误分支 VERSION_NOT_FOUND/SNAPSHOT_MISSING/FILE_NOT_FOUND 且失败不动文件 ④回溯清理同文件 pending ⑤崩溃恢复孤儿清理 cleanedSnapshots=2；`paragraph-diff.test.ts` 10 测试含末段替换合并单条 text 回归）
- **验收标准**：回溯后文件内容与快照一致；回溯操作本身也产生 ChangeSet（可再回溯）；无原子性漏洞。✔ 已验证（五道门禁全绿：typecheck node+web / lint / vitest 17 文件 118 测试 / build / e2e 10 用例含 `version:list` 空数组 + `version:restore` 结构化 VERSION_NOT_FOUND 冒烟）
- **关联**：架构设计文档 §5；PRD 3.3

---

### T-S2-07【P1】safeStorage 密钥管理

- [x] 使用 Electron `safeStorage` 加密存储 LLM API Key，调用位置严格限制在主进程（§3.6 约束）。（证据：`safe-storage-crypto.ts` SafeStorageCrypto 为 electron safeStorage 唯一封装落点——CryptoPort 注入式设计隔离 electron import，vitest 的 node 环境注入 FakeCrypto 即可全覆盖；`key-store.ts` KeyStoreService——密文（base64）经 atomicWriteFileSync（tmp→fsync→rename）原子落 `{userData}/secure/api-keys.json`、DB `model_configs` 行只存 `key-<uuid>` 引用、解密失败（keychain 重置形态）返回 null 降级不炸启动、safeStorage 不可用时拒绝明文落盘（KEYSTORE_UNAVAILABLE））
- [x] 实现多模型配置 UI：录入 / 更新 / 删除 API Key，渲染层通过 IPC 触发主进程操作，不在渲染层持有明文 Key。（证据：`model-config-service.ts` 编排 save/remove/setDefault/listViews + 密钥三态语义（省略=保持 / 空串=清除删条目 / 非空=覆写复用既有 ref）+ 首条自动默认 + remove 先删密文再删 DB 行（§5 清理顺序）；渲染层 `ModelConfigPanel.tsx` modal 面板——4 mutation 组件内闭环 + 2 enabled:open query、表单 password 输入（编辑时留空保持）、maskedKey 掩码展示、Provider 状态徽章（openai-compatible/mock+note）、加密不可用红色警示；IPC 白名单 5 通道 `model:list/save/delete/setDefault/status`（shared/ipc.ts 契约 + preload 桥接 + main/index.ts handler），视图窄字段（hasKey/maskedKey）零明文；主进程 `refreshProvider()` 配置变更后 `gateway.swapProvider` 热替换（UsageMeter 账本跨 swap 保留）+ providerStatus 更新；`provider-factory.ts` 优先级链：env 三元组 → DB 默认配置 + KeyStore 主进程解密 → Mock 诚实降级（note 说明原因））
- [x] 编写测试：验证 Key 加密后存储，主进程读取解密正常，渲染层 IPC 接口不暴露明文。（证据：`key-store.test.ts` 8 用例——FakeCrypto（AES-256-GCM，布局 iv+tag+密文）密文非明文落盘 / 解密往返 / 掩码长短两形态（>8 首尾各 4 字符、≤8 仅首 2 字符）/ 换 key 模拟 keychain 重置解密失败→null / deleteKey 与缺失分支 / 不可用拒绝明文；`model-config.test.ts` 11 用例（真实 SQLite + 进程内 dbAdapter 7 路由）——首条自动默认 + 视图序列化/DB 行/密文文件三处均不含明文、三态语义全分支（保持 ref 不变/覆写原地替换/清除删条目+置 null）、remove 清理顺序、setDefault 事务无双默认、MODEL_CONFIG_INVALID/NOT_FOUND 校验、KEYSTORE_UNAVAILABLE 宁可不保存、resolveChatProvider store 路由（解密成功→openai-compatible / 换 key 降级 mock+note 含「不完整」/ 无配置列出缺失项）；`smoke.spec.ts` +3 e2e 冒烟（model:list 返回数组、model:status 结构断言、model:save 空名→MODEL_CONFIG_INVALID 不写入数据））
- **验收标准**：API Key 不在渲染进程内存中明文存在；日志中无 Key 输出。✔ 已验证（五道门禁全绿：typecheck node+web / lint / vitest 20 文件 147 测试 / build / e2e 16 用例含三条新通道冒烟；明文不出主进程——IPC 契约只传窄字段视图、DB 只存引用、密文文件独立于 DB、provider-factory 日志仅记 model 与 mode 不含 Key）
- **关联**：架构设计文档 §3.6；PRD 7A.2

---

### T-S2-08【P1】三栏 UI 骨架

> 进展：E 项结构骨架 + 无障碍 + 布局契约先行落地；①FTS5 搜索、②mammoth 预览 + diff 高亮、③流式输出 + 工具调用状态指示三个内容项已全部补齐，五道门禁全绿。

- [x] 实现三栏布局（文件列表 / 对话 / 预览面板），响应式，最小宽度 1 280 px。（证据：`App.tsx` grid 五轨——左栏/把手/1fr/把手/右栏；主窗口 `minWidth:900`，wide≥1100 三栏并存）
- [x] 文件列表：显示导入文件、支持按名称/关键词搜索（调用 FTS5）。（证据：`db/search-repository.ts`——FTS5 虚表 + MATCH 词法守卫（用户输入统一转双引号字面短语、内部 `"` 转义 `""`，空串短路防 fts5 syntax error），trigram 分词支持中英文；`App.tsx` 左栏搜索框 250ms 防抖 → `file:search` IPC → `['file-search', q]` query，命中列表 + 「没有匹配」空态；`search-repository.test.ts` 10 用例）
- [x] 对话面板：消息气泡 / 流式输出 / 工具调用状态指示 / ChangeSet 卡片内嵌。（证据：流式三事件通道——`shared/ipc.ts` ChatStreamEvent（token / tool-start / tool-end）+ `chatSend` 第 3 参 turnId（渲染层 `crypto.randomUUID()` 生成，事件按轮归属，跨轮/迟到事件自然丢弃）+ `onChatStream` 订阅返回退订函数（effect cleanup 防监听器泄漏）；`main/index.ts` `e.sender.send('chat:stream')` 带 isDestroyed 守卫；`agent.ts` AgentToolEvent 起止事件（ok=`!cs.error` 仅错误 ChangeSet 判败，enforceConfirmation 拦截不算失败）callId 与 LlmToolCall.id 对齐；`mock-provider.ts` streamContent 6 字符/8ms 渐进流出（工具轮空 content 零 token）；`App.tsx` streamTurnRef 守卫 + 流式气泡 ▍ 光标（animate-pulse）+ toolLines 状态行（⏳ 运行中/✅ 成功/❌ 失败附错误文案），onSuccess 用结构化 finalMessage 覆盖流式文本（网关重试重复 token 由终值兜底）；ChangeSet 卡片 `ChangeSetCard.tsx`（T-S2-05 已落地）；`mock-provider.test.ts` 3 用例（渐进流出/工具轮零 token/无 opts 向后兼容）+ `agent-service.test.ts` 流式透传集成用例（token 拼接等于 finalMessage、tool-start/tool-end 按 callId 配对 ok:true））
- [x] 预览面板：Word 文件 mammoth HTML 预览，diff 高亮覆盖层。（证据：主进程 `preview:getHtml`——`word-parser.ts` mammoth 转换（file-worker 线程）→ `html-sanitizer.ts` 白名单消毒后才跨 IPC，渲染层 `WordPreviewPanel.tsx` 直接 dangerouslySetInnerHTML 注入（消毒是主进程职责，渲染层不做安全判断）；diff 覆盖层按 before.text 归一化（折叠空白）匹配预览段落（p/li/h1-h6/td/th/blockquote）加 `.mwo-pv-hit` 琥珀高亮——按文本而非段落索引匹配（表格/列表结构下索引不可靠对应），插入类无 before 文本无法定位属已知局限，图例如实展示「待审 N 处 · 高亮 M 处」（legendRef 直写 DOM 避开 set-state-in-effect）；`main.css` `.mwo-preview` 排版；`['preview-html', fileId]` 缓存 + changeSets 变更时失效；`word-parser.test.ts` + `html-sanitizer.test.ts` 覆盖（转换/消毒/高亮匹配），e2e `preview:getHtml` 未知文件结构化 FILE_NOT_FOUND 冒烟）
- [x] 实现键盘导航（Tab 序、焦点环可见、ARIA landmark），满足 WCAG AA 基线。（证据：`aside`/`section` 带 `aria-label` landmark；把手 `role="separator"` 可聚焦、focus-visible 环）
- [x] **【评审补录 #1】布局状态持久化**：栏宽 / 折叠态 / 输入框高度落 `ui_prefs`（localStorage 起步），跨会话恢复上次工作区，不随重启复位。（架构 §7.1）（证据：`store/ui-store.ts` persist name `mwo.ui-prefs`，partialize 仅持久 layout）
- [x] **【评审补录 #2】拖拽把手无障碍**：分隔条实现 `role="separator"` + `aria-valuenow/min/max`，支持方向键微调，命中区 ≥ 8px。（架构 §7.1）（证据：`ResizeHandle.tsx` role/aria-value* + Arrow/Home/End 键控 + step；命中区 2px 视觉线 + 8px 命中带）
- [x] **【评审补录 #3】危险操作 Undo 兜底**：应用变更 / 恢复版本写入 `undo` 状态，提供限时 Undo toast；`fast-pass` 信任模式下仍保留兜底。（架构 §7.1）（证据：`App.tsx` undo toast + `store` pushUndo/clearUndo/pruneUndo，TTL 10s）
- [x] **【评审补录 #4】响应式断点落地**：按 §7.2 实现 `wide/medium/narrow`（≥1100 / 900–1099 / <900）三档，自动折叠不覆盖用户手动布局偏好。（架构 §7.2）（证据：`store.applyWindowWidth` deriveBreakpoint + 非 wide 自动折叠右栏，仅在 wide 保留手动态）
- [x] **【评审补录 #9】未上线能力入口灰态**：知识库 / 生成初稿等 S3 能力入口在 S2 显示灰态占位，避免功能悬空。（评审 #9）（证据：左栏「知识库/生成初稿」disabled 虚线灰态按钮）
- **验收标准**：三栏可用；搜索有结果；对话流式输出正常；预览 diff 可见；布局跨重启保持；拖拽把手键盘可达；危险操作可 Undo；三种窗口宽度布局正确收敛。✔ 已验证（五道门禁全绿：typecheck node+web / lint / vitest 24 文件 176 测试 / build / e2e 17 用例含 file:search 通道、preview:getHtml、chat:send 三参形态冒烟；FTS5 中英文检索、token 渐进流出 + 工具状态行、琥珀高亮 diff 覆盖层均有单测覆盖）
- **关联**：架构设计文档 §2、§7.1、§7.2；PRD 2、2A

---

### ✅ S2 质量门禁

| 门禁项 | 标准 | 状态 |
| --- | --- | --- |
| 最小可信闭环端到端 | 导入→对话→ChangeSet→diff→确认→快照→回溯全链路跑通 | ✔ |
| 无静默改文件路径 | 所有文件写操作必须经 ChangeSet 确认 | ✔ |
| 原子性与崩溃恢复 | 启动扫描 pending ChangeSet + 孤立文件清理有覆盖测试 | ✔ |
| 隐私红线 | ContextBuilder 不发送完整文件；API Key 不在渲染层明文 | ✔ |
| 交互体验认可 | diff 预览/确认/部分接受/回退交互需求方可接受 | ✔ |
| 无阻断性技术障碍 | 无遗留 blocker issue | ✔ |

> 验证记录（2026-09-16，五道门禁全绿：typecheck node+web / lint / vitest 24 文件 177 测试 / build / e2e 17 用例）：

- [x] **① 最小可信闭环端到端**（证据：`agent/agent-service.test.ts` 新增闭环集成测试「S2 门禁① 最小可信闭环：导入→对话→ChangeSet→diff→确认→快照→回溯」，与主进程 index.ts 同一装配链 VersionService → TrustService → AgentService(trust)，逐链断言——runTurn 落 pending → listPendingViews 保留 sourceCommand → before/after 文本 diff → 确认前磁盘未变 → accept 后 appliedCount=1、磁盘出现新词 → v1 后像快照（author='ai'、摘要「AI 修改 1/1 项」、快照文件存在）→ 第二轮对话产生 v2 → getVersionDiff(v1) 恰 1 处替换（before/after 含新旧词）→ restore(v1) 后磁盘字节级等于 v1 快照 + 反向 ChangeSet 出生即 applied（source='manual'、sourceCommand「恢复到版本 v1」）+ v3（摘要「回溯至版本 v1」、parent=v2）→ 版本链长度 3、currentVersionId 指向恢复版本）
- [x] **② 无静默改文件路径**（证据：全量写入原语审计——`word.applyParagraphEdits` 唯一生产调用方 = trust-service.ts accept（前置 BASELINE_MISMATCH 校验 + expectedBefore 段前值匹配）；`file.copy` 唯一生产调用方 = version-service.ts 三处（onApplied 后像快照 / restore 替换工作文件 / restore 自身快照，均 .tmp→fsync→rename 原子写）；`writeFileSync` 生产代码零调用（仅测试）；`atomicWriteFileSync` 唯一生产调用方 = import-service.ts 导入副本创建（uuid 新文件名，不动用户原件）；external:accept/ignore 不写工作文件——采纳=审计行+快照+基线推进，忽略=纯 DB 重定基线；外部编辑检出时该文件 pending 全部置 stale，绝不静默覆盖；index.ts 22 个 IPC 通道逐一复核，写通道仅 importWord/accept/reject/restore/external 三键/model 三键，无旁路）
- [x] **③ 原子性与崩溃恢复**（证据：启动接线 index.ts L109 `recovery.run` → db/worker.ts → runCrashRecovery：pending 报告不 apply + tmpDir .tmp 残留清理 + 孤立 .changeset 外置文件清理 + snapshots 目录孤儿 .snapshot.docx 及写一半 .tmp 清理；覆盖测试——data-service.test.ts「crash recovery」2 用例（pending 报告、孤立外置清理）+ version-service.test.ts「孤儿快照与残留 .tmp 被清理（cleanedSnapshots），被引用快照保留」+ external-watch.ts 崩溃顺序设计（acceptExternal ①审计→②快照→③基线推进，中间崩溃由下轮扫描重放收敛，反序才是不可恢复漏洞）+ restore 失真 pending 按 §5 清理顺序丢弃用例）
- [x] **④ 隐私红线**（证据：ContextBuilder——context/context-builder.test.ts 7 用例（相关性选段只发命中段落、钉住仍占预算名额、单段超长截断打标、总量预算整段放弃不半段裁切、大文档 sentChars ≤ maxTotalChars 且 < totalDocChars 红线断言、空文档审计元数据归零）；API Key——key-store.test.ts 8 用例（密文落盘文件不含明文、掩码 `sk-l****wxyz` 格式、safeStorage 不可用抛 KEYSTORE_UNAVAILABLE 拒绝明文落盘、keychain 重置返回 null 降级）+ model-config.test.ts「视图零明文、DB 行只存引用、密文落盘」用例（`JSON.stringify(listViews())` 不含明文密钥）+ model-config-service.ts toView 仅暴露 hasKey/maskedKey + 渲染层 ModelConfigPanel 仅显示掩码（apiKey 只存在于用户新输入的出站表单）+ logger.ts SENSITIVE_KEYS 落盘脱敏双保险）
- [x] **⑤ 交互体验认可**（✔ 2026-09-16 用户验收通过并指令闭环 S2。验收载体：体验优化部署后的伪安装包（commit 950f0e5——真撤销 UndoAction 回滚链、加载骨架屏、入场动画、两步内联删除确认、根因修复包内 migrations 缺失导致的「功能点击不行」）；用户实际操作认可三栏闭环全链路：导入→对话流式输出→diff 琥珀高亮预览→ChangeSet 确认/拒绝→限时 Undo→版本回溯→外部编辑三选项）
- [x] **⑥ 无阻断性技术障碍**（证据：生产代码零 TODO/FIXME/XXX/HACK 标记（grep 复核）；全部「阻断」字样均为降级路径设计——批量导入单文件失败不阻断整批（PRD F1-1）、外部扫描失败不阻断启动（渲染层手动扫描按钮兜底）、safeStorage 不可用拒绝保存 Key、keychain 重置诚实降级 Mock 不阻断启动；已知限制均有 S3 规划——pageCount=null 待 LibreOffice 接入（架构 §4A.2、T-S3-03）、Excel/PPT 解析待 T-S2→S3 纵切扩展、插入类 diff 无 before 文本无法高亮定位属如实展示的已知局限）

---

## 阶段 S3：一期 MVP 开发

> 目标：在 S2 骨架基础上横向扩能力，实现 PRD 3.2.5 全部 P0 验收标准。
> 纵切完成后横切：Word 已通，扩 Excel，再扩 PPT。

### T-S3-01【P0】文件管理完整能力

- [ ] 支持批量导入（拖拽 / 多选），导入进度展示。
- [ ] 元信息编辑（标题、标签、备注），元信息同步写 FTS5 索引。
- [ ] 文件删除（软删除 + 确认弹窗，不可撤销操作需二次确认）。
- [ ] 本地全文检索：关键词高亮、结果排序、分页（对齐 SearchRepository 抽象）。
- [ ] 工作空间切换（多工作空间基础支持）。
- **关联**：PRD 3.1；架构设计文档 §3.6

---

### T-S3-02【P0】Excel 处理能力

- [ ] 导入 .xlsx，ExcelJS 读取，元信息写库，FTS5 索引单元格内容。
- [ ] 确定性操作：批量改单元格值、公式、格式（通过 Tool Registry 新增 ExcelCellTool）。
- [ ] diff 预览：单元格级着色（改前/改后值对比）。
- [ ] Excel HTML 预览（SheetJS → HTML）嵌入预览面板。
- [ ] 编写集成测试：导入→改单元格→diff 预览→确认→回溯。
- **关联**：架构设计文档 §3.4、§3.7；PRD 3.2

---

### T-S3-03【P0】PPT 处理能力

- [ ] 导入 .pptx，基于 T-S0-01 PoC 选定路径（python-pptx 主选 / LibreOffice 宏兜底）。
- [ ] 生成整套 PPT（pptxgenjs），大纲→幻灯片生成。
- [ ] 改写已有 PPT：修改文本、图片替换、形状属性。
- [ ] diff 预览：S2 分级（整页并排，形状级高亮留 S5）。
- [ ] LibreOffice 无头转 PDF/图 作为保真预览路径。
- [ ] 编写集成测试：导入→改写→diff 预览→确认→回溯。
- **关联**：架构设计文档 §3.4、§3.7；PRD 3.2

---

### T-S3-04【P0】Word 完整能力

- [ ] 扩展 Word 工具集（在 T-S2 基础上）：段落增删、标题级别调整、样式应用、表格操作。
- [ ] 保真预览：复杂排版走 LibreOffice 无头转 PDF → 渲染层展示。
- [ ] Word「页」定位（基于 T-S0-03 PoC 结论，LocationSelector page 定位可用）。
- [ ] 编写集成测试：覆盖 PRD 3.2.5 中 Word P0 验收标准。
- **关联**：架构设计文档 §3.4；PRD 3.2、4A.2

---

### T-S3-05【P0】核心 NFR 落地

- [ ] **性能基线**（PRD 7A.1）：
  - [ ] 冷启动 ≤ 3 s（Electron 主窗口可交互）。
  - [ ] 文件导入（≤ 10 MB）≤ 2 s。
  - [ ] 本地全文检索（10 000 文件）≤ 500 ms。
  - [ ] 确定性操作（非 LLM）≤ 1 s。
- [ ] **隐私红线**（PRD 7A.2）：数据流向日志（非明文内容，仅记录"发送 N 字符至 model X"），UI 可查。
- [ ] **原子性**（PRD 7A.3）：原子写 + 崩溃恢复路径在 S2 已覆盖，本阶段补充 Excel/PPT 写路径。
- **验收标准**：NFR 指标有自动化测试或可复现的测量脚本。

---

### T-S3-06【P1】LibreOffice 进程池完整实现

- [ ] Utility 进程中实现 LibreOffice 进程池（多实例 / 健康检查 / 崩溃隔离 / 超时回收 / 自愈重启）。
- [ ] 进程池 API：`convert(filePath, format): Promise<outputPath>`，调用方无感知实例管理。
- [ ] 编写压力测试：并发 5 个转换请求，进程崩溃后自动重启，无请求丢失。
- **关联**：架构设计文档 §3.4

---

### T-S3-07【P1】多模型配置完整能力

- [ ] 支持 OpenAI 兼容 / Anthropic / Ollama（本地）三类 Provider 配置。
- [ ] 模型切换不影响进行中的对话（中途切换给出提示）。
- [ ] UsageMeter：token 消耗展示（本次对话 / 累计），费用估算（已知模型单价）。
- **关联**：架构设计文档 §3.5；PRD 3.5

---

### T-S3-08【P1】核心链路测试覆盖

- [ ] 单元测试：Tool 契约、ChangeSet 序列化/反序列化、FTS5 SearchRepository、AtomicChange 部分接受逻辑、safeStorage 加解密。
- [ ] 集成测试：导入→对话→工具调用→ChangeSet→diff→确认→快照→回溯，三种文件格式各一条。
- [ ] E2E 测试（Playwright）：核心用户路径（导入文件 / 提问 / 确认变更 / 回溯）。
- **验收标准**：核心链路测试覆盖率达标（单元 ≥ 80%，集成关键路径全覆盖）；无 P0/P1 缺陷未闭环。

---

### T-S3-09【P1】知识提炼入库（K-1）

- [ ] 新增 `knowledge_entries` 表迁移（Drizzle，schema 已定义）+ SearchRepository 扩展知识条目 FTS5 索引。
- [ ] 提炼器：导入/处理成功后，从 Word/PPT/Excel 抽取 摘要 / 结构骨架 / 风格指纹 / 标签，写入 `knowledge_entries(status=suggested)`。
- [ ] 结构骨架抽取：Word 标题树、PPT 页序+页标题、Excel 表结构+字段（复用各格式解析器）。
- [ ] 风格指纹抽取：复用 FW-6/FP-6/FE-6 样式提取逻辑。
- [ ] 知识库 UI：条目列表 + 详情（来源文件、引用次数、置信度），支持确认/编辑/删除/降权（透明可控，对齐 3.4A）。
- **验收标准**：导入同类文档后能生成可见知识条目；用户可编辑/删除；敏感文件可标记不入库。
- **关联**：PRD 3.3A K-1；架构设计文档 §4

---

### T-S3-10【P1】知识持续进化与归并（K-2）

- [ ] 归并器：同 `category` 下结构骨架与风格指纹归并；模式出现 ≥ N 次（阈值可配）升为"推荐骨架/推荐风格"。
- [ ] `ref_count` / `confidence` 维护；来源文件删除或被用户否定时自动降权/失效。
- [ ] 知识库 UI 增强：展示"推荐骨架/推荐风格"及其来源与置信度。
- **验收标准**：反复导入同类文档后，同一 category 出现聚合的推荐骨架/风格；降权/失效逻辑可验证。
- **关联**：PRD 3.3A K-2；架构设计文档 §4

---

### T-S3-11【P1】基于知识生成同类初稿（K-3，生成性）

- [ ] 生成器（双来源）：① 按历史沉淀——选 category，用推荐骨架+风格+常用要点生成初稿；② 按新导入参考——即时提取参考文件目录结构与风格，套用到新主题生成（不要求先入库）。
- [ ] 生成产物落为新文件并进入标准信任流程（先出 diff/预览 → 确认 → 落盘 → 快照，复用 §5）。
- [ ] 支持多方案 / 逐节重生成 / 局部替换；生成依据（引用的知识条目/参考文件）可见可溯源。
- [ ] 编写集成测试：两种来源各生成一份初稿 → 预览 → 确认 → 快照。
- **验收标准**：能按历史类别或新导入参考生成结构/风格一致的初稿；产出不破坏文件结构、可拒绝回退。
- **关联**：PRD 3.3A K-3、4A.1（生成性操作）；架构设计文档 §4、§5

---

### T-S3-12【P2】UI 扩展性增强（评审补录 #5 / #8）

> 来源：S1 设计评审的可扩展性建议，非 MVP 阻塞项，随 Excel/PPT/知识能力铺开后落地。

- [ ] **【评审补录 #8】能力面板数据化**：右栏「上下文·连接器/技能」与 Skills/定时任务改为数据结构驱动（可增删），新增一个 Skill/连接器不改 UI 代码。（架构 §7.1 预览器注册表同构思路）
- [ ] **【评审补录 #5】预览独立分栏（可选模式）**：在四态互斥之外，提供"对话 + 预览同屏"的可选分栏模式（类 Claude Code draggable panes），面板抽象预留于 `UiState`。
- [ ] **【评审补录 #7 验证】预览器注册表落地**：`previewerRegistry[fileType] → Previewer` 组件映射投入使用，Word/Excel/PPT 走注册表，新增 PDF/Markdown 仅注册不改切换逻辑。（架构 §7.1）
- **验收标准**：新增一种文件类型/技能仅需注册、不改切换逻辑；可选分栏模式下对话与预览同屏且各自独立滚动。
- **关联**：架构设计文档 §7、§7.1；评审补录 #5/#7/#8

---

### T-S3-13【P1】手动优化回灌偏好/知识 · 第 3 层（对应 PRD 2A.6 第 3 层）

> 承接 T-S2-05A 捕获到的手动优化信号（`change_sets.source=manual/external` + `versions.author=user/external`），把其中**可泛化的模式**沉淀为偏好/知识。延续 §3.4A 可控优先：默认只建议、不自动学。

- [ ] **模式提炼**：对第 1/2 层的手动纠正做聚类，识别可泛化模式（同类改动 ≥ N 次），产出候选偏好/知识条目（附触发证据链：哪些文件、哪些 ChangeSet）。
- [ ] **第 3 层偏好回灌**：候选以**建议态**呈现（"是否记住这个偏好？"），用户确认后写入 `user_preferences`（复用 §3.4A 结构，rule/hit_count/weight/editable/deletable），支持查看/编辑/删除。
- [ ] **第 3 层知识回灌**：结构/风格类模式按 §3.3A K-1 提炼进知识库（`knowledge_entries`），标注来源为"用户手动优化"，与"文档提炼""显式设置"来源可区分。
- [ ] **接入度量口径**：回灌产生的偏好/知识纳入 §3.4D 度量（接受率/回退率/命中率/纠偏率），可被熔断降级。
- [ ] **可关闭**：提供独立开关，关闭后仅保留手动优化记录，不产生任何建议与回灌。
- [ ] 编写集成测试：构造 N 次同类手动纠正 → 触发建议 → 确认生效 → 偏好可编辑可删除；关闭开关后不再产生建议。
- **验收标准**：手动优化信号能收敛为可见可控的偏好/知识建议；未经用户确认不改变 AI 行为；用户可追溯到"这条偏好来自哪些改动"。
- **关联**：PRD 2A.6 第 3 层、3.3A K-1、3.4A、3.4D；架构设计文档 §4、§5.1、§7.3；上游 T-S2-05A

---

### ✅ S3 质量门禁

| 门禁项 | 标准 | 状态 |
| --- | --- | --- |
| PRD 3.2.5 P0 验收标准 | 全部通过 | `[ ]` |
| NFR 关键指标达标 | 冷启动/导入/检索/操作性能满足 7A.1 | `[ ]` |
| 隐私红线 | 7A.2 数据流向可审计 | `[ ]` |
| 原子性全格式覆盖 | Word/Excel/PPT 写路径均有原子写 + 崩溃恢复 | `[ ]` |
| 核心链路测试覆盖 | 单元 ≥ 80%；集成关键路径全覆盖；无 P0/P1 未闭环 | `[ ]` |

---

## 阶段 S4：一期测试与发布

### T-S4-01【P0】系统测试与 NFR 专项

- [ ] 性能专项：用真实文件（Word 50页/Excel 5 000行/PPT 30张）跑基线测量。
- [ ] 隐私专项：用网络抓包验证发送内容确为截断后的必要片段，无完整文件外传。
- [ ] 可靠性专项：模拟写入中途断电（kill -9），重启后验证崩溃恢复路径。
- [ ] 回归：对 PRD 3.2.5 全部验收标准执行一轮完整回归。

---

### T-S4-02【P0】UAT 与缺陷收敛

- [ ] 至少 2 名真实用户，用自己的真实办公文件跑核心路径。
- [ ] 收集反馈，P0/P1 缺陷全部闭环后方可发布。

---

### T-S4-03【P0】发布包构建与签名

- [ ] macOS：`electron-builder` 打包 .dmg，codesign + notarytool 公证，GateKeeper 验证放行。
- [ ] 准备回滚预案：保留上一版本安装包，文档描述回滚步骤。

---

## 阶段 S5：二期能力增强（概要，S4 后展开）

> 以下为方向性任务，S4 通过后再拆解详细子任务。

- [ ] 模板 / 风格匹配（3.2.4）
- [ ] PPT 美化（AI 配色 / 布局建议）
- [ ] Excel 可视化增强（图表生成 / 数据分析）
- [ ] 显式偏好记忆（3.4 A）+ 有限自检（3.4 B）
- [ ] 半确定性 / 生成性操作：多方案展示、迭代精炼
- [ ] Word「页」定位精化（4A.2 复杂文档兜底完善）
- [ ] ContextBuilder 可插拔性完整实现（contextHint 多策略）
- [ ] 可观测性（7A.4）：Agent 决策日志 UI 可查
- [ ] PPT diff shape 级高亮（S3 分级中的 S3 项）

---

## 阶段 S6：三期专业化与扩展（概要）

- [ ] 投标文件专业插件（Tool Registry 插件接入，child_process + JSON-RPC）
- [ ] 批量处理（多文件并行 ChangeSet）
- [ ] 自动进化（采纳率/回退率度量 → 开启条件）
  - 口径冻结于 PRD §3.4D + 架构 §7.3（本期不实现、不采集隐式行为）
  - [ ] 度量计算器（应用层只读派生：接受率/回退率/命中率/平均纠偏轮次，来源仅 `change_sets`/`versions`/`user_preferences`/`messages`）
  - [ ] 准入门槛判定（滚动 30 天且样本 ≥ 50；接受率 ≥ 70%、回退率 ≤ 10%、命中率 ≥ 60%、纠偏 ≤ 1.5 全满足）
  - [ ] 显式开启开关（达标不自动生效，用户确认后开启）+ 熔断回退（跌破门槛自动退回仅显式偏好记忆）
  - [ ] 进化日志（提级/降权可查看、可一键撤销）
  - 验收：样本不足时保持关闭并如实提示；任一指标跌破门槛一个窗口内自动熔断；进化日志可回看可撤销
- [ ] 云端协作扩展（Repository remote_id/etag/sync_state 字段启用）
- [ ] MCP 兼容接入（session/transport/capability，S4+，§7 澄清范围）

---

## 附录：§9.1 执行期重点关注清单（来自架构设计文档）

> 每个里程碑复盘时对照此表更新状态。

| 关注点 | 性质 | 判定/决策标准 | 关联任务 | 状态 |
| --- | --- | --- | --- | --- |
| python-pptx 打包+签名公证+体积 | S0 前置 PoC 黑盒 | 5 条标准全通过，或有明确兜底路径 | T-S0-01 | `[~]` 核心验证通过（改写✅ 体积33MB✅ 热路径136ms✅）；正式签名公证+Windows 待 S2 前置补验 |
| LibreOffice 随包 vs 预装 | S0 前置决策 | 有选定方案 + 体积基线数字 | T-S0-02 | `[ ]` |
| PPT diff 坐标映射精度 | S2/S3 分级 | S2 整页并排；S3 shape 级高亮（EMU→像素精度验证） | T-S3-03 | `[ ]` |
| SearchRepository 抽象落地 | 扩展性 | SqliteFtsSearchRepository 可替换，不侵入业务层 | T-S2-02 | `[x]` 接口 + FTS5 实现分离（`search-repository.ts`），业务层只依赖抽象 |
| ContextBuilder 可插拔性 | 扩展性 | contextHint 接口可覆盖默认裁剪策略 | T-S2-04 | `[x]` `buildContext` 接收 contextHint（`pinnedIndexes` 优先锁定 + `strategy` 透传），默认裁剪策略（评分选段/双预算）可整体替换，`replace-text-tool.test.ts` contextHint 用例 + `context-builder.test.ts` pinned 用例覆盖 |
| MCP 接入范围 | 预期管理，S4+ | 格式对齐≠可直接接入，S4 前不承诺 MCP 可用 | S6 | `[ ]` |
| 外置 changes 文件一致性 | 可靠性 | 先删文件后删 DB 行，有单元测试覆盖 | T-S2-02、T-S2-05 | `[x]` 双侧落地：数据层（`discardChangeSet` 先删文件后改状态、启动扫描清孤立文件，`data-service.test.ts`）；信任流（reject / supersede 双路径外置删除断言，`trust-service.test.ts` 外置 513KB 用例） |
| safeStorage 调用位置 | 约束 | 只在主进程调用，渲染层 IPC 接口不暴露明文 | T-S2-07 | `[ ]` |

---

*文档结束。本清单与《架构设计文档.md》（v1.2）、《需求文档.md》（PRD v2.1）、《后续工作计划.md》（v1.1）配套使用。*
