# 快速上手指南（Quick Start）

> 版本 v1.1 · 2026-09-16 · S2 闭环当日编写；v1.1 补齐仓库获取方式、开发平台前置、S3 准出门禁与 S4–S6 剩余路线总览
> 目标读者：接手本项目的工程师或 AI 协作 Agent。目标：**10 分钟建立全局认知**——项目在哪、是什么、怎么跑起来、下一步做什么、坑在哪。

---

## 1. 项目是什么（一句话）

**My-work-office**：交互式桌面办公智能体（Electron），围绕 Word / PPT / Excel 三类文档的**处理、生成、优化**，以对话式交互驱动，双轨文件引擎（JS 库快路径 + LibreOffice/python-pptx 保真路径）+ 可回滚变更集（ChangeSet）机制。

**当前状态**（2026-09-16）：

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| S0 技术预研 | ✅ | 5 个 PoC 全部有结论，结论已回灌架构文档 v1.3 |
| S1 设计冻结 | ✅ | 工具契约（§3.3）+ 数据模型（§4）M1 冻结 |
| S2 原型 | ✅ **已闭环** | 六项门禁全过（含门禁⑤ 用户验收）；Word 最小可信闭环端到端可用 |
| S3 MVP 开发 | ⏳ 当前阶段 | **下一个任务：T-S3-01 文件管理完整能力【P0】** |
| S4 测试发布 / S5 能力增强 / S6 专业化 | 未启动 | 见《后续工作计划.md》 |

当前已实现能力（S2 口径）：Word .docx 导入 → FTS5 中英文检索 → 对话流式输出 → AI 修改生成 ChangeSet → diff 琥珀高亮预览 → 确认/拒绝/部分接受 → 版本快照 + 线性回溯 + 限时真撤销 → 外部编辑检测（采纳/忽略/看 diff）→ 多模型 API Key 加密配置。**Excel/PPT 解析属 S3 范围（T-S3-02/03），当前导入对话框只放行 .docx/.doc。**

---

## 2. 治理文档（唯一事实来源，动手前先读）

| 文档 | 角色 |
| --- | --- |
| [需求文档.md](./需求文档.md)（PRD v2.1） | 产品需求、范围、验收口径 |
| [架构设计文档.md](./架构设计文档.md)（HLD v1.3，M1 已冻结） | 进程模型、引擎选型、数据/变更模型 |
| [后续工作计划.md](./后续工作计划.md) | S0–S6 阶段划分 + 阶段质量门禁 |
| [开发任务清单.md](./开发任务清单.md)（**开发主基准**） | 可执行任务分解 T-Sx-xx，每项带 `[x]` 证据 |

铁律：**代码不得偏离文档口径；若需偏离，先改文档再改代码**。每完成一项，在清单勾 `[x]` 并追加「证据：文件/用例/提交号」，禁止无证据勾选。

---

## 3. 仓库目录总览（根目录）

**获取代码**：`git clone https://github.com/charleswjwen-dotcom/jw-work-office.git`（远端 origin，`main` 分支始终可运行）。当前开发机本地路径（macOS，路径含空格，命令中必须加引号）：`/Users/jinwen/Library/Application Support/TRAE SOLO/ModularData/ai-agent/work-mode-projects/6aa12a74490f1076283fd4a4`。

```
.
├── 快速上手指南.md      # 本文件（新接手者从这里开始）
├── 需求文档.md / 架构设计文档.md / 后续工作计划.md / 开发任务清单.md   # 治理四文档
├── README.md            # 工程管理规范（分支模型 / Conventional Commits / 入库红线）
├── app/                 # ★ Electron 应用工程（npm 工作根，详见 §4）
├── poc/                 # S0 技术预研（一子目录一个 PoC，各带结论报告）
│   ├── s0-01-pptx/              # python-pptx onedir 打包 + Electron 进程池热路径
│   ├── s0-02-libreoffice-dist/  # LibreOffice 分发决策（选定 Homebrew cask 预装引导）
│   ├── s0-03-word-pagination/   # Word 页码→段落映射（soffice 转 PDF 定位）
│   ├── s0-04-word-diff/         # 段落级 diff / 部分接受 / 回滚
│   └── s0-05-llm-tool-control/  # LLM 可控性（工具调用强制确认）
├── tech-feasibility-report/     # 技术可行性报告（HTML 成品）
├── ui-design/           # 高保真设计稿（index.html 三栏规格 / main-screen.html 单屏稿）
└── .trae-html-share-packages/   # 文档分享归档（可忽略）
```

---

## 4. app/ 工程详解

```
app/
├── package.json              # 脚本与依赖（命令见 §6）
├── electron.vite.config.ts   # main / preload / renderer 三进程构建配置
├── drizzle.config.ts         # Drizzle 迁移生成配置（npm run db:generate）
├── playwright.config.ts / e2e/smoke.spec.ts   # E2E 冒烟（17 用例）
├── vendor/better-sqlite3/    # ★ 双 ABI 原生产物（勿删！见 §9）
├── dist-test/                # 伪安装包（git 忽略；部署产物，见 §7）
└── src/
    ├── main/                 # 主进程（Node 侧全部业务逻辑）
    │   ├── index.ts          # ★ IPC 装配中枢：22 个通道注册 + 进程装配
    │   ├── agent/            # 对话代理：runTurn → 工具调用 → 生成 ChangeSet
    │   ├── context/          # ContextBuilder：隐私预算选段（只发命中段落）
    │   ├── db/               # 数据层：worker(Utility进程) / schema(10表) / migrations / FTS5 / recovery(崩溃恢复)
    │   ├── files/            # 文件 worker(Utility进程)：mammoth 解析 / html-sanitizer 消毒 / word-writer
    │   ├── import/           # Word 导入编排（对话框→解析→入库→索引）
    │   ├── llm/              # LLMGateway / openai+mock Provider / model-config / usage-meter
    │   ├── security/         # API Key 加密存储（safeStorage + 密文落盘）
    │   ├── tools/            # Tool Registry + ReplaceTextTool（唯一写文件原语）
    │   ├── trust/            # ★ 信任交互核心：trust-service(确认) / version-service(快照回溯) / external-watch(外部编辑)
    │   └── logger.ts         # pino 日志 + SENSITIVE_KEYS 落盘脱敏
    ├── preload/index.ts      # contextBridge 白名单（渲染层唯一入口，新增 IPC 必须同步此处）
    ├── renderer/src/         # React UI
    │   ├── App.tsx           # ★ 三栏布局 + 全部交互接线（~955 行，单文件聚集）
    │   ├── components/       # chat/(ChangeSet卡·版本历史·预览·外部变更) layout/(ResizeHandle) settings/(模型配置) ui/(Button)
    │   ├── store/            # Zustand：ui-prefs 持久化 + Undo 状态（UndoAction 真撤销）
    │   ├── lib/              # query-client(TanStack Query) / inline-diff / utils
    │   └── assets/main.css   # 设计 token（24 个 OKLCH）+ mwo-* 专有样式
    └── shared/               # ★ 跨进程契约（改 IPC 三处联动之首）
        ├── ipc.ts            # 全部 IPC 通道类型定义
        ├── db-protocol.ts / file-protocol.ts   # 主进程↔Utility 进程协议
        └── paragraph-diff.ts # 段落 diff 纯函数（主/渲染共用）
```

---

## 5. 架构速览

**进程模型（架构 §2，故意隔离勿合并）**：

```
┌─ main（主进程）─── IPC 装配 / Agent / Trust / 版本快照
│    ├─ db-worker（UtilityProcess.fork）── SQLite 读写（better-sqlite3 + Drizzle）
│    └─ file-worker（UtilityProcess.fork）── mammoth 解析等 CPU 密集操作
├─ preload ── contextBridge 白名单（渲染层无 Node 能力）
└─ renderer（React 18 + Zustand + TanStack Query）
```

**核心数据流（最小可信闭环，S2 已跑通）**：

导入 .docx（原子写副本落 userData/files）→ files 表 + FTS5 索引 → 对话（ContextBuilder 选段 → LLM 流式 → ReplaceTextTool）→ **pending ChangeSet**（磁盘未动）→ diff 琥珀高亮预览 → 用户确认（trust-service.accept：基线校验→写文件→版本快照）→ 可回溯 / 限时 Undo（10s 真撤销：restore-version + anchor 缓存）。

**四条不变量（红线，评审必查）**：

1. **无静默改文件**：所有文件写操作必经 ChangeSet 确认（`word.applyParagraphEdits` 唯一生产调用方 = trust-service）。
2. **原子写**：`.tmp → fsync → rename`（`db/atomic-write.ts`），崩溃可恢复（启动扫描 pending + 孤立文件清理）。
3. **隐私**：ContextBuilder 只发命中段落不发全文；API Key 只存密文引用，渲染层只见掩码；日志脱敏。
4. **M1 冻结契约**：工具契约（架构 §3.3）与数据模型（§4）变更需版本化，不可静默改。

---

## 6. 环境搭建与常用命令

前置：**macOS 开发机**（userData 路径、伪安装包 .app、better-sqlite3 原生预编译均按 darwin 走；Windows/Linux 未验证）；Node 18+（项目用 22）+ npm + git。

```bash
cd app
npm install
npm run rebuild:electron   # ★ 装完必跑（better-sqlite3 Electron ABI，见 §9）
```

**五道质量门禁（每个任务完成必跑，全绿才算完成）**：

```bash
npm run typecheck   # 双 tsc（node + web 两个 project）
npm run lint        # eslint 严格模式（含 react-hooks 新规则）
npm test            # vitest（24 文件 177 测试）
npm run build       # electron-vite 生产构建（产物 app/out/）
npm run test:e2e    # Playwright（17 用例；建议连跑两次验证迁移幂等）
```

其他：`npm run dev`（开发热重载）/ `npm run db:generate`（改 schema 后生成迁移）/ `npm run rebuild:node`（还原 Node ABI，一般不手动用）。

---

## 7. 伪安装包部署（本机体验真实包）

不打包 dmg，直接用 Electron 官方壳 + 注入构建产物：

```bash
cd app
npm run build        # 先构建（app/out/ 是部署源）
rm -rf dist-test/MyWorkOffice.app/Contents/Resources/app/out
cp -R out dist-test/MyWorkOffice.app/Contents/Resources/app/
open -na "$(pwd)/dist-test/MyWorkOffice.app"   # ★ open 必须绝对路径，相对路径报错
```

用户数据目录：`~/Library/Application Support/my-work-office/`（db / files / changesets / snapshots / secure / tmp）。
历史教训：曾因包内缺 `migrations/` 导致数据层 NOT_READY、界面功能全不可用——**部署后必查包内 out/main/migrations/ 存在，且 userData 目录正常生成**。

---

## 8. 快速熟悉路径（推荐 30 分钟阅读序）

1. 本指南 §1–§5（全局认知）
2. 《需求文档.md》§3.2 / §3.2.5（能力范围与 P0 验收口径）
3. 《架构设计文档.md》§2（进程模型）→ §3.3（工具契约，冻结）→ §4（数据模型，冻结）→ §5（原子性与恢复）
4. 按数据流读代码：`shared/ipc.ts` → `main/index.ts` → `main/agent/agent-service.ts` → `main/trust/trust-service.ts` → `main/db/schema.ts` → `renderer/src/App.tsx`
5. 《开发任务清单.md》定位最近 `[x]` 与下一个 `[ ]`（配合 `git log --oneline -5` 看最近提交），直接开工

---

## 9. 关键约定与坑（必读速查）

| # | 约束/坑 | 说明 |
| --- | --- | --- |
| 1 | **better-sqlite3 锁 12.11.1** | 禁升 v13（N-API 10 vs Electron 33 内置 Node 20 的 N-API 9 → SIGSEGV）；`vendor/better-sqlite3/` 勿删；`npm install` 后必跑 `npm run rebuild:electron` |
| 2 | **注释写架构意图** | 中文注释讲"为什么这样设计"，标识符英文；关键约束标「勿删」+ 任务号引用 |
| 3 | **不引入新库** | 只用 package.json 已声明依赖；新增前先看邻近文件模式 |
| 4 | **set-state-in-effect** | eslint 规则禁 effect 体内同步 setState；放事件回调或 setTimeout 等异步回调 |
| 5 | **Tailwind 颜色是 var() 字符串** | `bg-red/80` 之类透明度修饰符不可用；武装/强调态用实底色类 |
| 6 | **迁移必须幂等** | `connection.ts` 的 `__mwo_migrations` 记账 + 基线检测机制勿破坏 |
| 7 | **`open -na` 要绝对路径** | 启动 .app 用相对路径会报 "Unable to find application" |
| 8 | **IPC 三处联动** | 新增通道：`shared/ipc.ts`（类型）+ `preload/index.ts`（白名单）+ `main/index.ts`（handler），缺一即断 |
| 9 | **提交规范** | Conventional Commits：`feat/fix/docs/refactor/test/chore(scope): 中文简述`；不提交构建产物/密钥/数据文件 |
| 10 | **分支模型** | `main` 受保护始终可运行；特性分支 `feat/<任务ID>-<简述>` |

---

## 10. 继续开发工作流（任务驱动，六步循环）

```
① 开工：读《开发任务清单.md》→ 定位下一个 [ ]（当前：T-S3-01 文件管理完整能力【P0】）
② 设计：对照 PRD 对应章节 + 架构对应 §；若需偏离文档口径 → 先改文档
③ 实现：遵循 §9 约定；新增能力在渲染层入口留灰态（S2 评审 #9 惯例）
④ 验证：五道门禁全绿（typecheck×2 / lint / vitest / build / e2e）
⑤ 落账：清单勾 [x] + 证据（文件/用例/提交号）；有偏差诚实披露
⑥ 提交：Conventional Commits → main（必要时部署伪安装包体验）
```

**S3 任务优先级**（详见《开发任务清单.md》阶段 S3 节）：
P0 = T-S3-01 文件管理 → T-S3-02 Excel → T-S3-03 PPT → T-S3-04 Word 完整 → T-S3-05 核心 NFR；
P1 = LibreOffice 进程池 / 多模型 / 核心链路测试 / 知识 K-1~K-3 / 手动回灌；P2 = UI 扩展性。

**S3 准出门禁（里程碑 M3「MVP 就绪」）**：PRD 3.2.5 全部 P0 验收标准通过；NFR 关键指标达标（性能 7A.1 / 隐私红线 7A.2 / 原子性 7A.3）；核心链路测试覆盖达标、无 P0/P1 缺陷未闭环。

**剩余阶段总览**（S4 起详见《后续工作计划.md》第 2 节）：

| 阶段 | 核心内容 | 准出门禁（一句话） |
| --- | --- | --- |
| S3 一期 MVP（**当前**） | Word/Excel/PPT P0 能力 + 文件管理 + 信任交互 + 多模型 + NFR | P0 验收全过 + NFR 达标 |
| S4 一期测试发布 | 系统测试 / NFR 专项 / 真实用户 UAT / 缺陷收敛 / 发布包构建签名 | 缺陷闭环 + UAT 通过 + 隐私复核 + 回滚预案就绪 |
| S5 二期能力增强 | 模板风格匹配 / PPT 美化 / 偏好记忆与有限自检 / 生成性操作多方案迭代 / 可观测性 | 新能力有验收 + 生成性操作可控可回退 |
| S6 三期专业化扩展 | 投标专业模块（插件接入 Tool Registry）/ 批量处理 / 自动进化（度量验证）/ 云端协作与 MCP | 插件独立验收 + 进化有度量证据 + 不破坏契约与红线 |

里程碑主线：M0 需求冻结 ✅ → M1 设计冻结 ✅ → M2 闭环验证 ✅ → **M3 MVP 就绪（当前推进中）** → M4 一期发布 → M5 能力增强 → M6 专业化。

---

## 11. 关键文件速查表（想做什么 → 看哪里）

| 意图 | 入口文件 |
| --- | --- |
| 加/改 IPC 通道 | `shared/ipc.ts` + `preload/index.ts` + `main/index.ts` |
| 改数据模型 | `src/main/db/schema.ts` → `npm run db:generate`（迁移入 `db/migrations/`） |
| 加 Agent 工具 | `src/main/tools/registry.ts`（参照 replace-text-tool.ts 模式） |
| 改隐私选段策略 | `src/main/context/context-builder.ts` |
| 改确认/回溯逻辑 | `src/main/trust/trust-service.ts` / `version-service.ts` |
| 改 UI 布局/交互 | `renderer/src/App.tsx`（三栏 + 接线）+ `components/` |
| 改设计 token | `renderer/src/assets/main.css`（OKLCH）+ `tailwind.config.js`（var 映射） |
| 检索行为 | `src/main/db/search-repository.ts`（FTS5 trigram + 词法守卫） |
| 外部编辑检测 | `src/main/trust/external-watch.ts` |
| LLM 接入 | `src/main/llm/gateway.ts` / `provider-factory.ts` / `model-config-service.ts` |
