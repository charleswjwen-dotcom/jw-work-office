# My-work-office 架构设计文档（HLD）

> 版本：v1.2
> 日期：2026-09-10
> 状态：待评审确认
> 关联文档：《需求文档.md》（PRD v2.1）、《后续工作计划.md》（S1 阶段交付物）
> 阶段定位：本文档为 S1「架构与详细设计」的核心产物，对应 M1 设计冻结里程碑；聚焦"系统怎么搭、各模块选什么、如何保证先进性与可扩展"。
>
> **v1.1 修订（据专业评审）**：
> - P0 补齐"改已有 PPT"技术路径（python-pptx 主选 + LibreOffice 宏兜底，§3.4，标注 S0 必做 PoC）；新增 §3.7「diff 预览架构」给出 Word/Excel/PPT 三格式渲染路径与 `ChangeSet.changes[]` 结构。
> - P1 明确 Agent 循环/文件引擎/LibreOffice **必须**运行于 Utility 进程（§2）；LibreOffice 进程池增加崩溃隔离与自愈（§3.4）；确立"单文件同一时刻仅一个 pending ChangeSet"约束（§5）。
> - P2 密钥存储主选改 `safeStorage`（keytar 已停维护）；`change_sets.changes` 增加大小阈值与外置文件策略（§4）；补齐主进程↔Utility 通信协议（§2）与启动崩溃恢复路径（§5）。
> - P3 插件改为独立 child_process + JSON-RPC 隔离（§3.3）；Repository 层预留云端并发/同步字段（§3.6）。
>
> **v1.2 修订（据第三方可执行性/可扩展性评审）**：
> - 执行性：细化 python-pptx 的 5 条 S0 PoC 成功标准，明确 LibreOffice 随包 vs 预装的 S0 前置决策（§3.4）；PPT diff 分级为 S2 整页并排 / S3 shape 级高亮以规避 EMU→像素映射精度风险（§3.7）；补齐外置 changes 文件与 DB 的清理一致性、safeStorage 主进程调用约束（§4/§5/§3.6）。
> - 扩展性：新增 `SearchRepository` 抽象隔离 FTS5 与业务层（§3.6/§8）；ContextBuilder 支持工具通过 `contextHint` 覆盖裁剪策略（§3.1/§3.3）；澄清 MCP "格式对齐≠可直接接入"的实际范围（§7）。
> - 新增 §9.1「执行期重点关注清单」汇总上述关注点与判定标准，供 S0/S2 落地时对照。

---

## 0. 设计目标与约束

### 0.1 设计目标（对齐 PRD 与用户诉求）

1. **先进性**：技术栈选用当前主流且有活跃生态的方案，避免选型即过时。
2. **可扩展**：新增一种文件操作 = 注册一个工具，不改核心；数据/存储/模型层为云端协作、海量文件、MCP 预留扩展位（对齐第 8 节确认事项）。
3. **可信可控**：所有 AI 变更走 ChangeSet → diff 预览 → 确认，绝不静默改文件（对齐 2A）。
4. **确定性优先**：能用代码确定性完成的操作绝不交给 LLM 猜（对齐 4A.1）。
5. **本地优先与隐私红线**：数据默认不出本机，密钥加密，数据流向透明（对齐 7A.2）。

### 0.2 硬约束（来自 PRD，不可违背）

| 约束 | 来源 | 设计含义 |
| --- | --- | --- |
| 桌面应用（Electron） | PRD 0/4.1 | 主/渲染进程分离架构 |
| 开源库为主 + LibreOffice 兜底 | PRD 0/4.3/第8节③ | 文件引擎双轨：JS 库快路径 + LibreOffice 保真路径 |
| macOS 优先，跨平台可迁移 | 第8节④ | 依赖选型避免平台锁定，打包先 mac |
| 本地单机优先，预留云端/共享 | 第8节① | 存储层抽象 Repository，可替换实现 |
| 上千文件，预留海量 | 第8节⑤ | 检索走独立索引，非全表扫描 |
| OpenAI 兼容 + Anthropic + 图像生成 + 本地模型 | 3.5/第8节⑦ | 统一 LLM 适配层，多协议 Provider |
| 隐私红线：仅发送必要片段 | 7A.2 | 上下文裁剪器 + 数据流向审计 |

---

## 1. 技术选型总览（含先进性与可扩展理由）

| 领域 | 选型 | 备选/兜底 | 选择理由（先进性 & 可扩展） |
| --- | --- | --- | --- |
| 桌面框架 | **Electron**（PRD 指定） | Tauri（远期评估） | 生态成熟、Node 主进程可直接跑文件处理库；Tauri 体积小但 Rust 生态对 Office 库支持弱，暂不选 |
| 构建/打包 | **electron-vite + electron-builder** | — | Vite 冷启动与 HMR 快，ESM 原生；builder 支持 mac 签名公证、后续多平台产物 |
| 前端框架 | **React 18 + TypeScript 5** | — | PRD 指定；并发特性、生态最大、类型安全 |
| UI 组件/样式 | **Tailwind CSS + shadcn/ui（Radix）** | Ant Design | 无运行时、可深度定制以对标 Claude Cowork；Radix 无障碍与主题/深浅色天然支持 |
| 状态管理 | **Zustand + TanStack Query** | Redux Toolkit | Zustand 轻量管本地 UI 态；Query 管异步/缓存/重试，契合大量 IPC 数据拉取 |
| 进程通信 | **Electron IPC + contextBridge（typed）** | — | preload 白名单暴露 API，渲染层零 Node 权限，满足安全基线 |
| Word 处理 | **docx（写）+ mammoth（转 HTML 预览）+ 原始 OOXML 操作** | LibreOffice | 确定性改写走 OOXML/docx；复杂保真预览走 LibreOffice |
| Excel 处理 | **ExcelJS（读写+样式+图表）** | SheetJS（大文件快读） | ExcelJS 支持样式/公式/图表写入；超大文件用 SheetJS 流式读兜底 |
| PPT 处理 | **pptxgenjs（生成）+ officeparser/自研 OOXML 解析（读改）** | LibreOffice | 生成整套 PPT 用 pptxgenjs；改已有 PPT 走 OOXML |
| 在线预览 | **快路径：mammoth/HTML + SheetJS→HTML + PPT→图；保真路径：LibreOffice --headless 转 PDF/图** | — | 双路径：常规文件秒开，复杂排版走 LibreOffice 保真（第8节③） |
| 分页/「页」定位 | **LibreOffice 无头转换取权威分页** + LocationSelector 抽象 | — | 解决 4A.2 Word「页」难题 |
| 本地数据库 | **SQLite（better-sqlite3）** | — | 同步 API、单文件、事务保证原子性（7A.3）；主进程独占访问 |
| 全文/元信息检索 | **SQLite FTS5** | 远期 Orama/外部索引 | FTS5 原生全文检索，上千级毫秒响应；海量时可切外部索引（第8节⑤） |
| ORM/数据访问 | **Drizzle ORM** | Prisma | Drizzle 轻量、TS 类型强、迁移可控、无额外运行时引擎 |
| LLM 适配 | **统一 LLMProvider 抽象**：OpenAI 兼容 / Anthropic / Ollama / 图像生成 | Vercel AI SDK | 自封装 Provider 便于精细控制 Function Calling、流式、上下文裁剪与审计 |
| 图像生成 | **文生图 Provider（OpenAI Images / SD 兼容 / 可本地）** | — | 满足 FP-4 AI 配图（第8节⑦） |
| 密钥存储 | **Electron `safeStorage`（OS 级加密，主选）** | keytar（已停维护，仅评估） | Electron 官方内建、跟随 OS keychain/DPAPI 加密，避免 keytar 停维护与原生编译风险；密钥密文落盘，DB 仅存引用（7A.2） |
| 日志/可观测 | **pino（结构化本地日志，脱敏）** | — | 排障与效果度量（7A.4） |
| 测试 | **Vitest（单元）+ Playwright（E2E）** | — | Vite 原生、快；Playwright 驱动 Electron E2E |
| 插件契约 | **JSON Schema（Zod 定义）+ Tool Registry**，向 MCP 靠拢 | — | 4B 契约、未来 MCP 兼容 |

> 选型总原则：**核心链路用成熟稳定方案，扩展点用标准协议预留**——既保证先进不落伍，又不因追新引入风险。

---

## 2. 系统分层架构

在 PRD 4.2 分层基础上细化为「三进程 + 六层」结构，明确 Electron 进程边界。

```
┌──────────────────────── Renderer Process（渲染进程，无 Node 权限） ────────────────────────┐
│  L1 展示层（UI）                                                                            │
│   三栏布局：文件列表 | 会话 | 预览工件区                                                     │
│   组件库 shadcn/ui · 主题(浅/深/自定义) · diff 预览 · 任务计划可视化 · 工件卡片              │
│  L2 前端应用层                                                                              │
│   Zustand(UI态) · TanStack Query(IPC数据) · 会话编排 UI · 快捷指令 · 空状态引导              │
└───────────────────────────────────── contextBridge(typed IPC) ─────────────────────────────┘
                                          ▲  白名单 API，参数校验
┌──────────────────────── Main Process（主进程，Node 权限，业务核心） ───────────────────────┐
│  L3 编排层（Orchestration）                                                                 │
│   IPC 路由 · 会话/任务调度器 · 上下文裁剪器(隐私) · 流式回传 · 中断控制                       │
│  L4 Agent 层                                                                                │
│   意图识别 · 工具选择 · Function Calling 循环 · 反思/自检 · 多方案生成                        │
│  L5 能力层（Tool Registry + 工具插件）                                                       │
│   Word/Excel/PPT 工具集 · 预览/分页 · 索引 · 快照 · 图像生成 ·〔投标插件槽·预留〕            │
│   —— 所有工具统一契约，产出 ChangeSet ——                                                    │
│  L6 基础设施层（Infrastructure）                                                            │
│   SQLite/Drizzle(Repository) · 文件系统(原子写) · LLM 适配(多Provider) · 密钥(keychain)      │
│   · 日志(pino) · LibreOffice 无头进程池                                                      │
└─────────────────────────────────────────────────────────────────────────────────────────┘
        │(可选，重解析/大文件)                                     │(外部)
        ▼                                                          ▼
  Utility Process（沙箱子进程：文件解析/LibreOffice 转换）   大模型服务 / 本地模型(Ollama)
```

**进程职责边界**
- **渲染进程**：纯 UI 与交互，禁用 `nodeIntegration`，只能通过 preload 白名单调用主进程；杜绝渲染层直接碰文件系统与密钥（安全红线）。
- **主进程**：IPC 路由、任务调度、SQLite 独占（事务与原子性）、ChangeSet 管理；不直接执行耗时操作。
- **Utility/子进程（必须）**：Agent Function Calling 循环、文件引擎（Word/Excel/PPT 解析改写）、LibreOffice 转换**必须**运行在 Utility 进程（Worker Threads 或独立 child_process），防止长时运算阻塞主进程 IPC 与 SQLite 响应（7A.1）。主进程只做调度与结果收发。

**主进程 ↔ Utility 通信协议**
- **Worker Threads**（同 Node 逻辑，如 Agent 循环、JS 文件库）：用 `MessagePort` + 结构化 `{ id, type, payload }` 请求/响应消息，大二进制（文件缓冲）走 `Transferable`（ArrayBuffer 转移）零拷贝。
- **独立 child_process**（异构运行时，如 python-pptx、LibreOffice 宏）：用 **JSON-RPC over stdio**，请求/响应带 `id` 关联，超时与错误统一封装；进程崩溃由主进程感知并按需重启。
- 所有跨进程消息类型集中定义于 `shared/`，双方共享 TS 类型，避免协议漂移。

---

## 3. 核心模块详细设计

### 3.1 编排层（Orchestrator）

职责：接收渲染层指令 → 组装上下文 → 驱动 Agent → 流式回传 → 管理中断。

关键子模块：
- **IPC Gateway**：所有 IPC channel 统一注册、入参 Zod 校验、错误规范化。
- **上下文裁剪器（ContextBuilder）★隐私核心**：根据 LocationSelector 只抽取"被操作的相关片段"送入 LLM，而非整份文件（落地 7A.2 红线）；同时产出"本次将发送给模型的内容"清单供 UI 透明展示。工具可通过 `Tool` 接口的可选 `contextHint?: ContextHint` 字段覆盖默认裁剪策略（如投标模块需要送入整份招标文件的特定章节时），ContextBuilder 优先使用工具提供的 hint，无 hint 时回退默认策略；这使裁剪逻辑可插拔，不需改 Orchestrator 核心。
- **任务调度器**：支持多步任务 plan→act→verify、可中断（AbortSignal 贯穿到 LLM 请求与工具执行）。
- **流式管道**：LLM token 流、工具执行进度、ChangeSet 生成事件经 IPC 增量推送到 UI。

### 3.2 Agent 层

- **意图解析**：将自然语言映射为「工具调用序列」，参数含 LocationSelector。
- **Function Calling 循环**：向 LLM 暴露 Tool Registry 中工具的 schema，模型选择工具与参数 → 主进程执行 → 结果回灌 → 直至完成或需用户确认。
- **确定性优先策略**（4A.1）：可被规则覆盖的意图直接路由到确定性工具，不进入 LLM 生成；仅生成性操作走 LLM 产内容。
- **反思/自检**：确定性操作用 `validate()` 规则校验；生成性操作产多方案供用户选择（对齐 3.4）。

### 3.3 能力层 —— 工具契约与 Tool Registry（4B 落地）

**工具接口（TypeScript 概念定义）**

```typescript
interface Tool<I = unknown> {
  name: string;                    // 'word.setFont' | 'excel.createChart' ...
  description: string;             // 供 LLM 理解
  category: 'deterministic' | 'semi' | 'generative';  // 4A.1 分类
  isDestructive: boolean;          // 决定是否强制确认(2A.1)
  preview: boolean;                // 是否支持 diff 预览
  inputSchema: ZodSchema<I>;       // JSON Schema，含 LocationSelector
  contextHint?: ContextHint;       // 可选：覆盖默认上下文裁剪策略(见 3.1)
  execute(input: I, ctx: ToolContext): Promise<ChangeSet>;  // 只产 ChangeSet，不落盘
  validate?(cs: ChangeSet): ValidationResult;               // 确定性自检
}
```

**Tool Registry（注册表）**
- 启动时扫描内置工具 + 已启用插件目录注册；核心调度层只依赖 `Tool` 抽象，不硬编码具体能力。
- 提供 `register / unregister / list / get / toSchemas(for LLM)`。
- **插件隔离**：插件运行在**独立 child_process**（非 vm/worker_threads），进程间通过 JSON-RPC over stdio 通信；声明式权限（可读写哪些文件类型）；主进程依据权限清单拦截越权调用，崩溃的插件进程不影响主进程。投标模块作为一组插件占位（第8节②，本期只留槽）。
- **MCP 兼容层**：Registry 输出的工具 schema 设计对齐 MCP tool 描述，未来加 `McpAdapter` 即可接入外部 MCP 工具/服务。

**LocationSelector（统一定位模型，4A.2）**

```typescript
type LocationSelector =
  | { type: 'paragraph'; index: number }
  | { type: 'heading'; text: string; level?: number }
  | { type: 'wordPage'; page: number }      // 依赖 LibreOffice 分页映射，附"排版相关"提示
  | { type: 'slide'; index: number; shapeId?: string }
  | { type: 'cellRange'; sheet: string; range: string }  // A1
  | { type: 'namedRange'; name: string };
```
- `wordPage` 由预览层/ LibreOffice 分页结果映射到段落范围，并向用户明示"页码基于当前排版"。

### 3.4 文件处理引擎（双轨）

| 格式 | 快路径（JS 库） | 保真/兜底路径 | 生成能力 |
| --- | --- | --- | --- |
| Word | docx 改写 OOXML + mammoth 预览 | LibreOffice 转 PDF/图取权威分页与保真预览 | — |
| Excel | ExcelJS 读写/样式/图表；SheetJS 流式读大文件 | LibreOffice | — |
| PPT | **python-pptx（改已有，主选）** / 自研 OOXML 解析（细粒度兜底） | LibreOffice 预览 / 宏批处理 | **pptxgenjs 从大纲生成整套 PPT**（FP-7，生成性） |
| 配图 | — | — | **图像生成 Provider 文生图**（FP-4） |

- **LibreOffice 无头进程池**：常驻 `soffice --headless` 进程池复用，降低单次转换冷启动开销；转换任务在 Utility 进程排队。
  - **崩溃隔离与自愈**：进程池带健康检查（心跳/存活探测）；单个 `soffice` 实例崩溃或转换超时（默认阈值）时，主管进程回收该实例、重启补足池容量，并对失败任务重试一次后降级为"预览不可用"提示，绝不拖垮主进程或其他任务。
  - 每个转换任务隔离到独立工作目录，避免临时文件互相污染。
  - **⚠️ 分发与版本决策（S0 前置，不可推迟到 S2）**：LibreOffice 采用**随包分发 + 版本锁定**（约 300MB，随 app 固定版本，规避用户环境差异）还是**要求用户预装**（安装包小但依赖用户环境、保真度不可控），直接决定安装包体积、签名/公证流程与用户体验，须在 S0 落决策。若随包：macOS 上 Gatekeeper 要求对捆绑的 `soffice` 二进制及其 dylib 单独签名与公证，须纳入 PoC 验证范围。
- **大文件策略**：>阈值文件走流式解析 + 分块渲染 + 懒加载缩略/预览（7A.1）。

> ⚠️ **PPT 改写高风险点（S0 必须 PoC 验证）**：`pptxgenjs` 仅能**生成**新文件，无法修改已有 PPT；而手写 OOXML 解析改写复杂度极高。因此"改已有 PPT"确定采用以下路径，按优先级兜底：
> 1. **python-pptx（主选）**：成熟稳定，支持读改文本框/表格/图片/占位符等常见元素；通过子进程（`child_process` 调用打包内置的 Python 运行时）执行，产出结构化变更映射为 ChangeSet。代价：需随包分发 Python 运行时，增加安装体积。
> 2. **LibreOffice 宏批处理（兜底）**：对 python-pptx 覆盖不到的复杂版式，用 `soffice` 宏（Basic/UNO）脚本化改写，复用既有 LibreOffice 依赖。代价：改写粒度粗、可控性弱。
> 3. **自研 OOXML 局部改写（受限场景）**：仅对"替换文本/换图"等确定性强、结构简单的操作直接改 XML，避免重型依赖。
> S0 阶段须对 python-pptx 的元素定位精度、样式保真、跨平台运行时打包做 PoC，结论回灌本节再冻结。
>
> **S0 PoC 成功标准（须全部满足才判定路径可行）**：
> 1. **完整链路跑通**：Electron 子进程调打包内置的 Python 运行时执行 python-pptx，成功修改一个真实 `.pptx`（改标题/正文文本），改后样式保真（字体/字号/颜色/占位符位置不丢失）。
> 2. **打包可分发**：Python 运行时用 standalone 分发包（如 `python-build-standalone`）嵌入，不依赖用户系统 Python；`electron-builder` `extraResources` 带入并可在打包后的 `.app` 内正确定位与 spawn。
> 3. **macOS 签名公证通过**：嵌入的 Python 解释器及其 `.so` 扩展（lxml/Pillow 等）均完成签名，整包公证（notarization）通过，Gatekeeper 不拦截。
> 4. **体积可接受**：python-pptx + 依赖 + Python 运行时增量体积（约 60–100MB）经产品确认在可接受范围。
> 5. **Windows 预研**：记录 Windows 侧路径分隔、MSVC runtime、杀毒误报等已知差异与应对，作为后续跨平台的输入（本期不必跑通，但须评估）。

### 3.5 LLM 适配层（多 Provider）

```
LLMGateway
 ├─ ChatProvider（OpenAI 兼容 | Anthropic | Ollama/vLLM 本地）
 ├─ ImageProvider（文生图：OpenAI Images | SD 兼容 | 本地）
 ├─ 统一能力：Function Calling 归一化、流式、重试/降级、超时中断
 ├─ 上下文裁剪对接（只传必要片段）
 └─ UsageMeter（记录 tokensIn/out、次数、成本 → UsageRecord）
```
- **协议归一化**：OpenAI 与 Anthropic 的 tool-calling 字段差异在 Provider 内屏蔽，Agent 层面向统一接口。
- **故障降级**（7A.3）：单 Provider 不可用→提示并支持切换默认模型；网络中断→明确错误+重试。
- **本地模型**：涉密场景（如投标）走 Ollama/vLLM，实现"数据不出内网"。

### 3.6 数据与存储层（Repository 模式，为云端预留）

- 采用 **Repository 接口 + SQLite 实现**：`FileRepository / VersionRepository / ConversationRepository ...`。
- 本期实现 `SqliteXxxRepository`；未来共享/云端只需提供 `RemoteXxxRepository`，上层不变（落地第8节①可扩展要求）。
- **检索独立为 `SearchRepository` 抽象**：全文/元信息检索通过 `SearchRepository` 接口访问，本期实现 `SqliteFtsSearchRepository`（FTS5）；海量文件时切换 `OramaSearchRepository` 或外部索引实现即可，业务层不感知（对齐第8节⑤）。避免 FTS5 调用散落在业务层导致未来切换需改多处。
- **Drizzle** 定义 schema 与迁移；所有多步写操作包裹事务，保证原子性。
- **云端预留字段**：核心表（`files/versions/change_sets`）预留 `remote_id`、`etag/rev`（乐观并发）、`sync_state(local/synced/dirty/conflict)`、`updated_by`；本期不启用，避免未来共享化改表迁移成本。
- **密钥读写位置约束**：`safeStorage` 的加解密是**同步 API**，但单次耗时极短（<1ms），必须在**主进程**调用（依赖 Electron 会话与 OS keychain 访问权限）；不得迁入 Worker/Utility 进程，否则会破坏 keychain 访问权限——此为有意约束，非"主进程不做耗时操作"原则的例外遗漏。

---

### 3.7 diff 预览架构（信任交互的核心 UI 支撑）★

diff 预览是 2A 信任流程的关键界面，架构层必须给出可实现路径，否则 S2 原型会在此卡壳。核心思路：**ChangeSet 是唯一事实源，diff 是对 `changes[]` 的按格式渲染，不依赖对整份文件做文本 diff。**

**ChangeSet.changes[] 结构（跨格式统一）**

```typescript
interface AtomicChange {
  id: string;                       // 单条变更 id，供部分接受勾选
  location: LocationSelector;       // 复用 4A.2 定位模型
  kind: 'text' | 'style' | 'insert' | 'delete' | 'cell' | 'element' | 'image';
  before?: ChangePayload;           // 改前快照(可空=纯新增)
  after?: ChangePayload;            // 改后内容(可空=纯删除)
  accepted?: boolean;               // 部分接受标记
  renderHint?: 'inline' | 'sideBySide' | 'thumbnail';
}
```
- 工具 `execute()` 产出的 ChangeSet **必须携带 before/after 结构化载荷**，diff 渲染只消费该结构，不做整文件对比 → 保证性能与准确性。

**三格式渲染策略**

| 格式 | diff 单元 | 渲染方式 | 数据来源 |
| --- | --- | --- | --- |
| Word | 段落/文本片段（run 级） | 富文本内联高亮（增/删/改用色标），复杂排版切 side-by-side（左原文 HTML / 右改后 HTML，mammoth 渲染） | before/after 为段落 OOXML 片段→转 HTML |
| Excel | 单元格 | 表格视图中被改单元格着色 + tooltip 展示旧值→新值；区域改动圈选 | before/after 为 `{addr, value, style}` |
| PPT | 幻灯片元素（shape） | 缩略图并排（改前/改后整页渲染）+ 元素级高亮框；文本改动可展开内联 | before/after 为元素属性 + LibreOffice 渲染的页图 |

> ⚠️ **PPT diff 实现分级（EMU→像素坐标映射存在精度风险）**：PPT shape 坐标系为 EMU 单位，与 LibreOffice 导出图片的像素坐标需要精确映射；字体替换、系统字体差异等会导致 LibreOffice 渲染布局偏移，使 shape 高亮框错位。因此采用如下分级策略：
> - **S2 原型**：PPT diff 只做**整页并排对比**（改前页图 / 改后页图左右展示），完全规避坐标映射问题，快速验证信任交互主流程。
> - **S3 迭代**：在整页并排基础上叠加 **shape 级高亮框**（EMU→像素坐标映射 + 偏移校正），作为体验增强项；若 LibreOffice 渲染一致性无法保证精度，可降级为"文本变更摘要列表"代替高亮框。

**关键设计点**
- **渐进增强**：文本类改动优先内联 diff（体验最佳）；结构/排版类改动降级为 side-by-side / 缩略图对比，保证任何变更都可预览。
- **部分接受**：UI 勾选映射到 `changes[].accepted`，应用时只落被勾选项（对齐 §5 partial 分支）。
- **渲染位置**：diff 计算/结构化在主进程完成，渲染进程只做展示，避免渲染层触达文件。
- S2 原型须先打通 Word 段落级内联 diff 这一最高频路径，再补 Excel/PPT。

---

## 4. 数据模型落地（PRD 第 5 章 → 物理表设计）

SQLite 主要表（Drizzle schema 概念）：

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| `workspaces` | id, name, created_at, settings(json) | 工作空间，settings 含默认模型/偏好作用域 |
| `files` | id, workspace_id(FK), name, type, path, size, page_count, sheet_count, tags(json), thumbnail, current_version_id, imported_at, modified_at | 文件元信息，检索来源 |
| `versions` | id, file_id(FK), seq, created_at, trigger_command, author, change_summary, storage_type(full/diff), snapshot_path, change_set_id, parent_version_id | 线性快照；每 N 版落全量、其间存增量 |
| `change_sets` | id, file_id(FK), source_command, status(pending/applied/discarded/partial), changes(json), changes_path(text nullable) | 打通信任流程与版本管理的核心结构；`changes` 超过阈值（建议 512 KB）时写外部 `.changeset` 文件，`changes_path` 存文件路径，`changes` 字段置 null，避免 SQLite 行膨胀 |
| `conversations` | id, workspace_id(FK), title, created_at, updated_at | 会话持久化 |
| `messages` | id, conversation_id(FK), role, content, tool_calls(json), change_set_id, result_cards(json), created_at | 消息，关联 ChangeSet 与工件卡片 |
| `user_preferences` | id, scope(global/workspace/filetype), rule, hit_count, weight, editable, deletable | 用户可见可管理的偏好 |
| `model_configs` | id, name, protocol, base_url, model, api_key_ref(keychain引用), is_default | 密钥仅存 keychain 引用，不落库明文 |
| `usage_records` | id, model_id, tokens_in, tokens_out, call_count, cost, timestamp | 用量与成本 |
| `fts_files` (FTS5) | file_id, content, metadata | 全文/元信息检索虚拟表 |

**索引策略**：`files(workspace_id, type)`、`versions(file_id, seq)`、`messages(conversation_id)`、FTS5 全文索引；上千级毫秒响应，海量时可迁外部索引。

---

## 5. 信任交互与 ChangeSet 数据流（2A 落地）

这是"可信可控"的主链路，也是全系统最关键的数据流：

```
用户指令
  → 编排层组装上下文(裁剪器：仅相关片段)
  → Agent 选择工具(Function Calling)
  → 工具 execute() 产出 ChangeSet(status=pending，不落盘)
  → IPC 回传渲染层 → 生成 diff 预览(按 LocationSelector 高亮改前/改后)
  → 用户操作分支：
        ├─ 全部接受 → apply 全量 → 写临时文件→原子替换→生成 Version 快照→ status=applied
        ├─ 部分接受 → 标记 changes[].accepted → 仅应用勾选项 → status=partial → 新快照
        ├─ 拒绝     → 丢弃 → status=discarded → 文件不变
        └─ 澄清追问 → 携带上下文继续对话，生成新 ChangeSet
  → 任意时刻可中断(AbortSignal)；已应用的可经版本线性回溯撤销
```

**关键设计点**
- 破坏性工具（`isDestructive`）强制走确认，不可静默应用。
- ChangeSet 是预览、部分接受、快照、回溯四者共用的唯一事实源，避免数据不一致。
- **单文件并发约束**：同一文件在任一时刻**只允许一个 `pending` ChangeSet**；新指令若命中已有 pending 的文件，需先让用户处理（接受/拒绝）或显式覆盖旧 ChangeSet，防止两份未落盘变更基于同一基线互相冲突。
- **崩溃恢复**：应用启动时扫描 `status=pending` 的 ChangeSet 与残留 `.tmp` 文件——pending ChangeSet 重新加载供用户处理（不自动应用），孤立 `.tmp` 一律清理；因 rename 是原子操作，文件本身要么是旧版要么是新版，不会半写，恢复后数据一致。
- **外置 changes 文件一致性**：ChangeSet 若使用外置 `.changeset` 文件（§4，超阈值），丢弃/清理时**先删外置文件、再删数据库行**；启动扫描阶段一并清理"有文件无对应行"或"行 discarded 但文件残留"的孤立文件，保证 DB 与文件系统一致。
- 原子性：`落盘 = 写 .tmp → fsync → rename 替换`；快照写成功后才更新 `current_version_id`（7A.3）。

---

## 6. NFR 可达性论证（对应 7A，供 S1 门禁）

| NFR | 目标 | 架构保障手段 |
| --- | --- | --- |
| 冷启动 < 3s | 7A.1 | Vite 产物 + 主进程懒加载工具/LibreOffice 进程池延迟启动 |
| 导入预览 <2s / 大文件不卡 | 7A.1 | 快路径预览 + Utility 进程解析 + 分块懒加载 |
| 检索 <1s | 7A.1 | SQLite FTS5 索引，非全表扫描 |
| 确定性操作 <2s | 7A.1 | 纯代码工具执行，不经 LLM 往返 |
| 隐私红线 | 7A.2 | 上下文裁剪器仅传相关片段 + 数据流向清单 UI + 本地存储 + keychain 密钥 + 本地模型选项 |
| 可靠性/原子性 | 7A.3 | 临时文件+rename、事务、快照校验回退、Provider 降级 |
| 可观测性 | 7A.4 | pino 脱敏日志 + UsageRecord + 效果度量指标留存 |
| 跨平台可迁移 | 第8节④ | 依赖避免平台锁定，LibreOffice/keychain 用跨平台封装 |

---

## 7. 可扩展性设计（先进性与演进保障）

| 扩展维度 | 预留机制 |
| --- | --- |
| 新增文件操作 | 实现 `Tool` 契约并注册，核心零改动（4B） |
| 投标专业模块 | 插件槽 + 权限声明，三期独立开发（第8节②） |
| 外部工具/服务 | Registry schema 向 MCP 对齐，加 `McpAdapter` 接入。**注意**："对齐 schema 格式"≠"直接可接入 MCP"：真正接入还需补充 MCP session 管理、transport（stdio/SSE）、capability negotiation。现阶段目标是格式兼容，`McpAdapter` 实现留 S4+ 迭代；不应被误读为"今天即可接 MCP 工具/服务" |
| 上千 → 海量文件 | 检索层从 FTS5 切换外部索引，`SearchRepository` 替换实现（第8节⑤） |
| 新模型/图像模型 | 新增 `Provider` 实现，配置即用 |
| 本地 → 云端/团队共享 | Repository 接口替换为远程实现，上层不变（第8节①） |
| 上千 → 海量文件 | 检索层从 FTS5 切换外部索引，Repository 分页/分片（第8节⑤） |
| 多平台 | electron-builder 增加 Windows/Linux target，依赖已跨平台 |

---

## 8. 目录结构建议（Monorepo）

```
my-work-office/
├─ packages/
│  ├─ main/            # 主进程：orchestrator, agent, registry, infra
│  │  ├─ orchestrator/
│  │  ├─ agent/
│  │  ├─ tools/        # 内置工具(word/excel/ppt/preview/index/snapshot/image)
│  │  ├─ registry/
│  │  ├─ llm/          # Provider 适配
│  │  ├─ repositories/ # Repository + Drizzle schema + SearchRepository(FTS5)
│  │  └─ infra/        # sqlite, fs-atomic, keychain, logger, libreoffice
│  ├─ preload/         # typed contextBridge API
│  ├─ renderer/        # React 前端(三栏/预览/diff/主题)
│  └─ shared/          # 跨进程类型：Tool, ChangeSet, LocationSelector, DTO
├─ plugins/            # 外部工具插件(投标模块预留)
├─ resources/          # LibreOffice 打包相关
└─ tests/              # vitest + playwright
```

---

## 9. 待进一步细化的详细设计（S1 后续 & 需评审确认）

1. **各工具的 inputSchema 明细**（P0 工具逐个定义 schema 与 validate 规则）。
2. **diff 预览的可视化规格**（Word 段落级内联、Excel 单元格级着色；PPT 按 §3.7 分级：S2 整页并排、S3 shape 级高亮）。
3. **LibreOffice 打包与分页映射算法**的 PoC 验证（S0 高风险点，建议先行）。
4. **UI 高保真设计稿**（对标 Claude Cowork，三栏 + 工件卡片 + 任务计划可视化）。
5. **插件权限模型细化**（独立 child_process + JSON-RPC 下的权限声明与拦截规则）。
6. **`ContextHint` 契约定义**（工具自定义裁剪策略的结构与默认回退规则，§3.1）。

### 9.1 执行期重点关注清单（据第三方评审，S0/S2 落地时对照）

| 关注点 | 性质 | 判定/决策标准 | 关联章节 |
| --- | --- | --- | --- |
| python-pptx 打包 + 签名公证 + 体积 | S0 前置 PoC（执行黑盒） | 必须满足 §3.4 列出的 5 条成功标准全部通过才判路径可行 | §3.4 |
| LibreOffice 随包分发 vs 要求预装 | S0 前置决策 | 权衡安装体积/签名复杂度/保真度/用户体验，S0 落定 | §3.4 |
| PPT diff 坐标映射精度 | S2/S3 分级 | S2 只做整页并排规避风险；shape 级高亮移 S3，精度不达标则降级为文本摘要 | §3.7 |
| SearchRepository 抽象落地 | 扩展性保障 | FTS5 调用不得散落业务层，统一走 `SearchRepository` | §3.6 |
| ContextBuilder 可插拔性 | 扩展性保障 | 投标等特殊模块通过 `ContextHint` 覆盖，不改 Orchestrator | §3.1 |
| MCP 接入范围 | 预期管理 | 现阶段仅格式对齐；session/transport/capability 留 S4+ | §7 |
| 外置 changes 文件一致性 | 可靠性 | 丢弃/启动扫描按"先删文件后删行"清理孤立文件 | §4/§5 |
| safeStorage 调用位置 | 约束 | 同步 API，必须留在主进程，不得迁 Worker | §3.6 |

---

*文档结束。本文档为 S1 架构基线草案，评审冻结后作为 M1 里程碑产物，指导 S2 原型开发。建议与 S0 高风险 PoC（Word 分页、保真度、LLM 可控性）结论互相校正后再冻结。*
